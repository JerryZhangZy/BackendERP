//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Web;
using System.Threading.Tasks;
using System.Xml.Serialization;
using Newtonsoft.Json;

using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.CommerceCentral.ERPDb;
using Microsoft.AspNetCore.Http;

namespace DigitBridge.CommerceCentral.ERPMdl
{
    /// <summary>
    /// Represents a SystemSettingService.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public class SystemSettingManager :  ISystemSettingManager , IMessage
    {

        public SystemSettingManager() : base() {}

        public SystemSettingManager(IDataBaseFactory dbFactory)
        {
            SetDataBaseFactory(dbFactory);
        }
        
        [XmlIgnore, JsonIgnore]
        protected SystemSettingService _systemSettingService;
        [XmlIgnore, JsonIgnore]
        public SystemSettingService systemSettingService
        {
            get
            {
                if (_systemSettingService is null)
                    _systemSettingService = new SystemSettingService(dbFactory);
                return _systemSettingService;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected SystemSettingDataDtoCsv _systemSettingDataDtoCsv;
        [XmlIgnore, JsonIgnore]
        public SystemSettingDataDtoCsv systemSettingDataDtoCsv
        {
            get
            {
                if (_systemSettingDataDtoCsv is null)
                    _systemSettingDataDtoCsv = new SystemSettingDataDtoCsv();
                return _systemSettingDataDtoCsv;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected SystemSettingList _systemSettingList;
        [XmlIgnore, JsonIgnore]
        public SystemSettingList systemSettingList
        {
            get
            {
                if (_systemSettingList is null)
                    _systemSettingList = new SystemSettingList(dbFactory);
                return _systemSettingList;
            }
        }

        public async Task<byte[]> ExportAsync(SystemSettingPayload payload)
        {
            var rowNumList =await systemSettingList.GetRowNumListAsync(payload);
            var dtoList = new List<SystemSettingDataDto>();
           foreach(var x in rowNumList)
            {
                if (systemSettingService.GetData(x))
                    dtoList.Add(systemSettingService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new SystemSettingDataDto());
            return systemSettingDataDtoCsv.Export(dtoList);
        }

        public byte[] Export(SystemSettingPayload payload)
        {
            var rowNumList =systemSettingList.GetRowNumList(payload);
            var dtoList = new List<SystemSettingDataDto>();
            foreach (var x in rowNumList)
            {
                if (systemSettingService.GetData(x))
                    dtoList.Add(systemSettingService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new SystemSettingDataDto());
            return systemSettingDataDtoCsv.Export(dtoList);
        }

        public void Import(SystemSettingPayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list = systemSettingDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.SystemSetting = item;
                    if (systemSettingService.Add(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in systemSettingService.Messages)
                            Messages.Add(msg);
                        systemSettingService.Messages.Clear();
                    }
                }
                if (payload.HasSystemSetting)
                    payload.SystemSetting = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        public async Task ImportAsync(SystemSettingPayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list =systemSettingDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.SystemSetting = item;
                    if (await systemSettingService.AddAsync(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in systemSettingService.Messages)
                            Messages.Add(msg);
                        systemSettingService.Messages.Clear();
                    }
                }
                if (payload.HasSystemSetting)
                    payload.SystemSetting = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        #region DataBase
        [XmlIgnore, JsonIgnore]
        protected IDataBaseFactory _dbFactory;

        [XmlIgnore, JsonIgnore]
        public IDataBaseFactory dbFactory
        {
            get
            {
                if (_dbFactory is null)
                    _dbFactory = DataBaseFactory.CreateDefault();
                return _dbFactory;
            }
        }

        public void SetDataBaseFactory(IDataBaseFactory dbFactory)
        {
            _dbFactory = dbFactory;
        }

        #endregion DataBase

        #region Messages
        protected IList<MessageClass> _messages;
        [XmlIgnore, JsonIgnore]
        public virtual IList<MessageClass> Messages
        {
            get
            {
                if (_messages is null)
                    _messages = new List<MessageClass>();
                return _messages;
            }
            set { _messages = value; }
        }
        public IList<MessageClass> AddInfo(string message, string code = null) =>
             Messages.Add(message, MessageLevel.Info, code);
        public IList<MessageClass> AddWarning(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Warning, code);
        public IList<MessageClass> AddError(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Error, code);
        public IList<MessageClass> AddFatal(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Fatal, code);
        public IList<MessageClass> AddDebug(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Debug, code);

        #endregion Messages
    }
}
