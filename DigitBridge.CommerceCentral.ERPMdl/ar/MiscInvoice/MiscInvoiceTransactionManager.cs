//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Web;
using System.Threading.Tasks;
using System.Xml.Serialization;
using Newtonsoft.Json;

using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.CommerceCentral.ERPDb;
using Microsoft.AspNetCore.Http;

namespace DigitBridge.CommerceCentral.ERPMdl
{
    /// <summary>
    /// Represents a MiscInvoiceTransactionService.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public class MiscInvoiceTransactionManager :  IMiscInvoiceTransactionManager , IMessage
    {

        public MiscInvoiceTransactionManager() : base() {}

        public MiscInvoiceTransactionManager(IDataBaseFactory dbFactory)
        {
            SetDataBaseFactory(dbFactory);
        }
        
        [XmlIgnore, JsonIgnore]
        protected MiscInvoiceTransactionService _miscInvoiceTransactionService;
        [XmlIgnore, JsonIgnore]
        public MiscInvoiceTransactionService miscInvoiceTransactionService
        {
            get
            {
                if (_miscInvoiceTransactionService is null)
                    _miscInvoiceTransactionService = new MiscInvoiceTransactionService(dbFactory);
                return _miscInvoiceTransactionService;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected MiscInvoiceTransactionDataDtoCsv _miscInvoiceTransactionDataDtoCsv;
        [XmlIgnore, JsonIgnore]
        public MiscInvoiceTransactionDataDtoCsv miscInvoiceTransactionDataDtoCsv
        {
            get
            {
                if (_miscInvoiceTransactionDataDtoCsv is null)
                    _miscInvoiceTransactionDataDtoCsv = new MiscInvoiceTransactionDataDtoCsv();
                return _miscInvoiceTransactionDataDtoCsv;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected MiscInvoiceTransactionList _miscInvoiceTransactionList;
        [XmlIgnore, JsonIgnore]
        public MiscInvoiceTransactionList miscInvoiceTransactionList
        {
            get
            {
                if (_miscInvoiceTransactionList is null)
                    _miscInvoiceTransactionList = new MiscInvoiceTransactionList(dbFactory);
                return _miscInvoiceTransactionList;
            }
        }

        public async Task<byte[]> ExportAsync(MiscInvoiceTransactionPayload payload)
        {
            var rowNumList =await miscInvoiceTransactionList.GetRowNumListAsync(payload);
            var dtoList = new List<MiscInvoiceTransactionDataDto>();
           foreach(var x in rowNumList)
            {
                if (miscInvoiceTransactionService.GetData(x))
                    dtoList.Add(miscInvoiceTransactionService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new MiscInvoiceTransactionDataDto());
            return miscInvoiceTransactionDataDtoCsv.Export(dtoList);
        }

        public byte[] Export(MiscInvoiceTransactionPayload payload)
        {
            var rowNumList =miscInvoiceTransactionList.GetRowNumList(payload);
            var dtoList = new List<MiscInvoiceTransactionDataDto>();
            foreach (var x in rowNumList)
            {
                if (miscInvoiceTransactionService.GetData(x))
                    dtoList.Add(miscInvoiceTransactionService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new MiscInvoiceTransactionDataDto());
            return miscInvoiceTransactionDataDtoCsv.Export(dtoList);
        }

        public void Import(MiscInvoiceTransactionPayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list = miscInvoiceTransactionDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.MiscInvoiceTransaction = item;
                    if (miscInvoiceTransactionService.Add(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in miscInvoiceTransactionService.Messages)
                            Messages.Add(msg);
                        miscInvoiceTransactionService.Messages.Clear();
                    }
                }
                if (payload.HasMiscInvoiceTransaction)
                    payload.MiscInvoiceTransaction = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        public async Task ImportAsync(MiscInvoiceTransactionPayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list =miscInvoiceTransactionDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.MiscInvoiceTransaction = item;
                    if (await miscInvoiceTransactionService.AddAsync(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in miscInvoiceTransactionService.Messages)
                            Messages.Add(msg);
                        miscInvoiceTransactionService.Messages.Clear();
                    }
                }
                if (payload.HasMiscInvoiceTransaction)
                    payload.MiscInvoiceTransaction = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        #region DataBase
        [XmlIgnore, JsonIgnore]
        protected IDataBaseFactory _dbFactory;

        [XmlIgnore, JsonIgnore]
        public IDataBaseFactory dbFactory
        {
            get
            {
                if (_dbFactory is null)
                    _dbFactory = DataBaseFactory.CreateDefault();
                return _dbFactory;
            }
        }

        public void SetDataBaseFactory(IDataBaseFactory dbFactory)
        {
            _dbFactory = dbFactory;
        }

        #endregion DataBase

        #region Messages
        protected IList<MessageClass> _messages;
        [XmlIgnore, JsonIgnore]
        public virtual IList<MessageClass> Messages
        {
            get
            {
                if (_messages is null)
                    _messages = new List<MessageClass>();
                return _messages;
            }
            set { _messages = value; }
        }
        public IList<MessageClass> AddInfo(string message, string code = null) =>
             Messages.Add(message, MessageLevel.Info, code);
        public IList<MessageClass> AddWarning(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Warning, code);
        public IList<MessageClass> AddError(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Error, code);
        public IList<MessageClass> AddFatal(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Fatal, code);
        public IList<MessageClass> AddDebug(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Debug, code);

        #endregion Messages
    }
}
