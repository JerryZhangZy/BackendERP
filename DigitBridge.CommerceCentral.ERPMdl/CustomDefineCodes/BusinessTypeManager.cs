//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Web;
using System.Threading.Tasks;
using System.Xml.Serialization;
using Newtonsoft.Json;

using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.CommerceCentral.ERPDb;
using Microsoft.AspNetCore.Http;

namespace DigitBridge.CommerceCentral.ERPMdl
{
    /// <summary>
    /// Represents a BusinessTypeService.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public class BusinessTypeManager :  IBusinessTypeManager , IMessage
    {

        public BusinessTypeManager() : base() {}

        public BusinessTypeManager(IDataBaseFactory dbFactory)
        {
            SetDataBaseFactory(dbFactory);
        }
        
        [XmlIgnore, JsonIgnore]
        protected BusinessTypeService _businessTypeService;
        [XmlIgnore, JsonIgnore]
        public BusinessTypeService businessTypeService
        {
            get
            {
                if (_businessTypeService is null)
                    _businessTypeService = new BusinessTypeService(dbFactory);
                return _businessTypeService;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected BusinessTypeDataDtoCsv _businessTypeDataDtoCsv;
        [XmlIgnore, JsonIgnore]
        public BusinessTypeDataDtoCsv businessTypeDataDtoCsv
        {
            get
            {
                if (_businessTypeDataDtoCsv is null)
                    _businessTypeDataDtoCsv = new BusinessTypeDataDtoCsv();
                return _businessTypeDataDtoCsv;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected BusinessTypeList _businessTypeList;
        [XmlIgnore, JsonIgnore]
        public BusinessTypeList businessTypeList
        {
            get
            {
                if (_businessTypeList is null)
                    _businessTypeList = new BusinessTypeList(dbFactory);
                return _businessTypeList;
            }
        }

        public async Task<byte[]> ExportAsync(BusinessTypePayload payload)
        {
            var rowNumList =await businessTypeList.GetRowNumListAsync(payload);
            var dtoList = new List<BusinessTypeDataDto>();
           foreach(var x in rowNumList)
            {
                if (businessTypeService.GetData(x))
                    dtoList.Add(businessTypeService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new BusinessTypeDataDto());
            return businessTypeDataDtoCsv.Export(dtoList);
        }

        public byte[] Export(BusinessTypePayload payload)
        {
            var rowNumList =businessTypeList.GetRowNumList(payload);
            var dtoList = new List<BusinessTypeDataDto>();
            foreach (var x in rowNumList)
            {
                if (businessTypeService.GetData(x))
                    dtoList.Add(businessTypeService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new BusinessTypeDataDto());
            return businessTypeDataDtoCsv.Export(dtoList);
        }

        public void Import(BusinessTypePayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list = businessTypeDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.BusinessType = item;
                    if (businessTypeService.Add(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in businessTypeService.Messages)
                            Messages.Add(msg);
                        businessTypeService.Messages.Clear();
                    }
                }
                if (payload.HasBusinessType)
                    payload.BusinessType = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        public async Task ImportAsync(BusinessTypePayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list =businessTypeDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.BusinessType = item;
                    if (await businessTypeService.AddAsync(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in businessTypeService.Messages)
                            Messages.Add(msg);
                        businessTypeService.Messages.Clear();
                    }
                }
                if (payload.HasBusinessType)
                    payload.BusinessType = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        #region DataBase
        [XmlIgnore, JsonIgnore]
        protected IDataBaseFactory _dbFactory;

        [XmlIgnore, JsonIgnore]
        public IDataBaseFactory dbFactory
        {
            get
            {
                if (_dbFactory is null)
                    _dbFactory = DataBaseFactory.CreateDefault();
                return _dbFactory;
            }
        }

        public void SetDataBaseFactory(IDataBaseFactory dbFactory)
        {
            _dbFactory = dbFactory;
        }

        #endregion DataBase

        #region Messages
        protected IList<MessageClass> _messages;
        [XmlIgnore, JsonIgnore]
        public virtual IList<MessageClass> Messages
        {
            get
            {
                if (_messages is null)
                    _messages = new List<MessageClass>();
                return _messages;
            }
            set { _messages = value; }
        }
        public IList<MessageClass> AddInfo(string message, string code = null) =>
             Messages.Add(message, MessageLevel.Info, code);
        public IList<MessageClass> AddWarning(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Warning, code);
        public IList<MessageClass> AddError(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Error, code);
        public IList<MessageClass> AddFatal(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Fatal, code);
        public IList<MessageClass> AddDebug(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Debug, code);

        #endregion Messages
    }
}
