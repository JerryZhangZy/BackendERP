//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Web;
using System.Threading.Tasks;
using System.Xml.Serialization;
using Newtonsoft.Json;

using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.CommerceCentral.ERPDb;
using Microsoft.AspNetCore.Http;

namespace DigitBridge.CommerceCentral.ERPMdl
{
    /// <summary>
    /// Represents a SystemCodesService.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public class SystemCodesManager :  ISystemCodesManager , IMessage
    {

        public SystemCodesManager() : base() {}

        public SystemCodesManager(IDataBaseFactory dbFactory)
        {
            SetDataBaseFactory(dbFactory);
        }
        
        [XmlIgnore, JsonIgnore]
        protected SystemCodesService _systemCodesService;
        [XmlIgnore, JsonIgnore]
        public SystemCodesService systemCodesService
        {
            get
            {
                if (_systemCodesService is null)
                    _systemCodesService = new SystemCodesService(dbFactory);
                return _systemCodesService;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected SystemCodesDataDtoCsv _systemCodesDataDtoCsv;
        [XmlIgnore, JsonIgnore]
        public SystemCodesDataDtoCsv systemCodesDataDtoCsv
        {
            get
            {
                if (_systemCodesDataDtoCsv is null)
                    _systemCodesDataDtoCsv = new SystemCodesDataDtoCsv();
                return _systemCodesDataDtoCsv;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected SystemCodesList _systemCodesList;
        [XmlIgnore, JsonIgnore]
        public SystemCodesList systemCodesList
        {
            get
            {
                if (_systemCodesList is null)
                    _systemCodesList = new SystemCodesList(dbFactory);
                return _systemCodesList;
            }
        }

        public async Task<byte[]> ExportAsync(SystemCodesPayload payload)
        {
            var rowNumList =await systemCodesList.GetRowNumListAsync(payload);
            var dtoList = new List<SystemCodesDataDto>();
           foreach(var x in rowNumList)
            {
                if (systemCodesService.GetData(x))
                    dtoList.Add(systemCodesService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new SystemCodesDataDto());
            return systemCodesDataDtoCsv.Export(dtoList);
        }

        public byte[] Export(SystemCodesPayload payload)
        {
            var rowNumList =systemCodesList.GetRowNumList(payload);
            var dtoList = new List<SystemCodesDataDto>();
            foreach (var x in rowNumList)
            {
                if (systemCodesService.GetData(x))
                    dtoList.Add(systemCodesService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new SystemCodesDataDto());
            return systemCodesDataDtoCsv.Export(dtoList);
        }

        public void Import(SystemCodesPayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list = systemCodesDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.SystemCodes = item;
                    if (systemCodesService.Add(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in systemCodesService.Messages)
                            Messages.Add(msg);
                        systemCodesService.Messages.Clear();
                    }
                }
                if (payload.HasSystemCodes)
                    payload.SystemCodes = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        public async Task ImportAsync(SystemCodesPayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list =systemCodesDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.SystemCodes = item;
                    if (await systemCodesService.AddAsync(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in systemCodesService.Messages)
                            Messages.Add(msg);
                        systemCodesService.Messages.Clear();
                    }
                }
                if (payload.HasSystemCodes)
                    payload.SystemCodes = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        #region DataBase
        [XmlIgnore, JsonIgnore]
        protected IDataBaseFactory _dbFactory;

        [XmlIgnore, JsonIgnore]
        public IDataBaseFactory dbFactory
        {
            get
            {
                if (_dbFactory is null)
                    _dbFactory = DataBaseFactory.CreateDefault();
                return _dbFactory;
            }
        }

        public void SetDataBaseFactory(IDataBaseFactory dbFactory)
        {
            _dbFactory = dbFactory;
        }

        #endregion DataBase

        #region Messages
        protected IList<MessageClass> _messages;
        [XmlIgnore, JsonIgnore]
        public virtual IList<MessageClass> Messages
        {
            get
            {
                if (_messages is null)
                    _messages = new List<MessageClass>();
                return _messages;
            }
            set { _messages = value; }
        }
        public IList<MessageClass> AddInfo(string message, string code = null) =>
             Messages.Add(message, MessageLevel.Info, code);
        public IList<MessageClass> AddWarning(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Warning, code);
        public IList<MessageClass> AddError(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Error, code);
        public IList<MessageClass> AddFatal(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Fatal, code);
        public IList<MessageClass> AddDebug(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Debug, code);

        #endregion Messages
    }
}
