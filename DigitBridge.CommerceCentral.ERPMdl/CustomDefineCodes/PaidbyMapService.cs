    
//-------------------------------------------------------------------------
// This document is generated by T4
// It will only generate once, if you want re-generate it, you need delete this file first.
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Web;
using System.Threading.Tasks;

using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.CommerceCentral.ERPDb;
using Newtonsoft.Json;

namespace DigitBridge.CommerceCentral.ERPMdl
{
    public partial class PaidbyMapService
    {

        #region override methods

        /// <summary>
        /// Initiate service objcet, set instance of DtoMapper, Calculator and Validator 
        /// </summary>
        public override PaidbyMapService Init()
        {
            base.Init();
            SetDtoMapper(new PaidbyMapDataDtoMapperDefault());
            SetCalculator(new PaidbyMapServiceCalculatorDefault(this,this.dbFactory));
            AddValidator(new PaidbyMapServiceValidatorDefault(this, this.dbFactory));
            return this;
        }

        /// <summary>
        /// Before update data (Add/Update/Delete). call this function to update relative data.
        /// For example: before save shipment, rollback instock in inventory table according to shipment table.
        /// Mostly, inside this function should call SQL script update other table depend on current database table records.
        /// </summary>
        public override async Task BeforeSaveAsync() 
        {
            try
            {
                await base.BeforeSaveAsync();
            }
            catch (Exception)
            {
                AddWarning("Updating relative data caused an error before save.");
            }
        }

        /// <summary>
        /// Before update data (Add/Update/Delete). call this function to update relative data.
        /// For example: before save shipment, rollback instock in inventory table according to shipment table.
        /// Mostly, inside this function should call SQL script update other table depend on current database table records.
        /// </summary>
        public override void BeforeSave() 
        {
            try
            {
                base.BeforeSave();
            }
            catch (Exception)
            {
                AddWarning("Updating relative data caused an error before save.");
            }
        }

        /// <summary>
        /// After save data (Add/Update/Delete), doesn't matter success or not, call this function to update relative data.
        /// For example: after save shipment, update instock in inventory table according to shipment table.
        /// Mostly, inside this function should call SQL script update other table depend on current database table records.
        /// So that, if update not success, database records will not change, this update still use then same data. 
        /// </summary>
        public override async Task AfterSaveAsync() 
        {
            try
            {
                await base.AfterSaveAsync();
            }
            catch (Exception)
            {
                AddWarning("Updating relative data caused an error after save.");
            }
        }

        /// <summary>
        /// After save data (Add/Update/Delete), doesn't matter success or not, call this function to update relative data.
        /// For example: after save shipment, update instock in inventory table according to shipment table.
        /// Mostly, inside this function should call SQL script update other table depend on current database table records.
        /// So that, if update not success, database records will not change, this update still use then same data. 
        /// </summary>
        public override void AfterSave() 
        {
            try
            {
                base.AfterSave();
            }
            catch (Exception)
            {
                AddWarning("Updating relative data caused an error after save.");
            }
        }

        /// <summary>
        /// Only save success (Add/Update/Delete), call this function to update relative data.
        /// For example: add activity log records.
        /// </summary>
        public override async Task SaveSuccessAsync()
        {
            try
            {
                await base.SaveSuccessAsync();
            }
            catch (Exception)
            {
                AddWarning("Updating relative data caused an error after save success.");
            }
        }

        /// <summary>
        /// Only save success (Add/Update/Delete), call this function to update relative data.
        /// For example: add activity log records.
        /// </summary>
        public override void SaveSuccess()
        {
            try
            {
                base.SaveSuccess();
            }
            catch (Exception)
            {
                AddWarning("Updating relative data caused an error after save success.");
            }
        }

        /// <summary>
        /// Sub class should override this method to return new ActivityLog object for service
        /// </summary>
        protected override ActivityLog GetActivityLog() => null;
            //new ActivityLog(dbFactory)
            //{
            //    Type = (int)ActivityLogType.Invoice,
            //    Action = (int)this.ProcessMode,
            //    LogSource = "PaidbyMapService",

            //    MasterAccountNum = this.Data.PaidbyMap.MasterAccountNum,
            //    ProfileNum = this.Data.PaidbyMap.ProfileNum,
            //    DatabaseNum = this.Data.PaidbyMap.DatabaseNum,
            //    ProcessUuid = this.Data.PaidbyMap.InvoiceUuid,
            //    ProcessNumber = this.Data.PaidbyMap.InvoiceNumber,
            //    ChannelNum = this.Data.PaidbyMap.ChannelAccountNum,
            //    ChannelAccountNum = this.Data.PaidbyMap.ChannelAccountNum,

            //    LogMessage = string.Empty
            //};

        #endregion override methods

        /// <summary>
        /// Add new data from Dto object
        /// </summary>
        public virtual bool Add(PaidbyMapDataDto dto)
        {
            if (dto is null) 
                return false;
            // set Add mode and clear data
            Add();

            if (!Validate(dto))
                return false;

            // load data from dto
            FromDto(dto);

            // validate data for Add processing
            if (!Validate())
                return false;

            return SaveData();
        }

        /// <summary>
        /// Add new data from Dto object
        /// </summary>
        public virtual async Task<bool> AddAsync(PaidbyMapDataDto dto)
        {
            if (dto is null)
                return false;
            // set Add mode and clear data
            Add();

            if (!(await ValidateAsync(dto)))
                return false;

            // load data from dto
            FromDto(dto);

            // validate data for Add processing
            if (!(await ValidateAsync()))
                return false;

            return await SaveDataAsync();
        }

        public virtual bool Add(PaidbyMapPayload payload)
        {
            if (payload is null || !payload.HasPaidbyMap)
                return false;

            // set Add mode and clear data
            Add();

            if (!ValidateAccount(payload))
                return false;

            if (!Validate(payload.PaidbyMap))
                return false;

            // load data from dto
            FromDto(payload.PaidbyMap);

            // validate data for Add processing
            if (!Validate())
                return false;

            return SaveData();
        }

        public virtual async Task<bool> AddAsync(PaidbyMapPayload payload)
        {
            if (payload is null || !payload.HasPaidbyMap)
                return false;

            // set Add mode and clear data
            Add();

            if (!(await ValidateAccountAsync(payload)))
                return false;

            if (!(await ValidateAsync(payload.PaidbyMap)))
                return false;

            // load data from dto
            FromDto(payload.PaidbyMap);

            // validate data for Add processing
            if (!(await ValidateAsync()))
                return false;

            return await SaveDataAsync();
        }

        /// <summary>
        /// Update data from Dto object.
        /// This processing will load data by RowNum of Dto, and then use change data by Dto.
        /// </summary>
        public virtual bool Update(PaidbyMapDataDto dto)
        {
            if (dto is null || !dto.HasPaidbyMap)
                return false;
            //set edit mode before validate
            Edit();
            if (!Validate(dto))
                return false;

            // load data 
            GetData(dto.PaidbyMap.RowNum.ToLong());

            // load data from dto
            FromDto(dto);

            // validate data for Add processing
            if (!Validate())
                return false;

            return SaveData();
        }

        /// <summary>
        /// Update data from Dto object
        /// This processing will load data by RowNum of Dto, and then use change data by Dto.
        /// </summary>
        public virtual async Task<bool> UpdateAsync(PaidbyMapDataDto dto)
        {
            if (dto is null || !dto.HasPaidbyMap)
                return false;
            //set edit mode before validate
            Edit();
            if (!(await ValidateAsync(dto)))
                return false;

            // load data 
            await GetDataAsync(dto.PaidbyMap.RowNum.ToLong());

            // load data from dto
            FromDto(dto);

            // validate data for Add processing
            if (!(await ValidateAsync()))
                return false;

            return await SaveDataAsync();
        }

        /// <summary>
        /// Update data from Payload object.
        /// This processing will load data by RowNum of Dto, and then use change data by Dto.
        /// </summary>
        public virtual bool Update(PaidbyMapPayload payload)
        {
            if (payload is null || !payload.HasPaidbyMap || payload.PaidbyMap.PaidbyMap.RowNum.ToLong() <= 0)
                return false;
            //set edit mode before validate
            Edit();

            if (!ValidateAccount(payload))
                return false;

            if (!Validate(payload.PaidbyMap))
                return false;

            // load data 
            GetData(payload.PaidbyMap.PaidbyMap.RowNum.ToLong());

            // load data from dto
            FromDto(payload.PaidbyMap);

            // validate data for Add processing
            if (!Validate())
                return false;

            return SaveData();
        }

        /// <summary>
        /// Update data from Dto object
        /// This processing will load data by RowNum of Dto, and then use change data by Dto.
        /// </summary>
        public virtual async Task<bool> UpdateAsync(PaidbyMapPayload payload)
        {
            if (payload is null || !payload.HasPaidbyMap)
                return false;
            //set edit mode before validate
            Edit();
            if (!(await ValidateAccountAsync(payload)))
                return false;

            if (!(await ValidateAsync(payload.PaidbyMap)))
                return false;

            // load data 
            await GetDataAsync(payload.PaidbyMap.PaidbyMap.RowNum.ToLong());

            // load data from dto
            FromDto(payload.PaidbyMap);

            // validate data for Add processing
            if (!(await ValidateAsync()))
                return false;

            return await SaveDataAsync();
        }

        /// <summary>
        ///  get data by number
        /// </summary>
        /// <param name="payload"></param>
        /// <param name="orderNumber"></param>
        /// <returns></returns>
        public virtual async Task<bool> GetDataAsync(PaidbyMapPayload payload, string orderNumber)
        {
            return await GetByNumberAsync(payload.MasterAccountNum, payload.ProfileNum, orderNumber);
        }

        /// <summary>
        /// get data by number
        /// </summary>
        /// <param name="payload"></param>
        /// <param name="orderNumber"></param>
        /// <returns></returns>
        public virtual bool GetData(PaidbyMapPayload payload, string orderNumber)
        {
            return GetByNumber(payload.MasterAccountNum, payload.ProfileNum, orderNumber);
        }

        /// <summary>
        /// Delete data by number
        /// </summary>
        /// <param name="orderNumber"></param>
        /// <returns></returns>
        public virtual async Task<bool> DeleteByNumberAsync(PaidbyMapPayload payload, string orderNumber)
        {
            if (string.IsNullOrEmpty(orderNumber))
                return false;
            //set delete mode
            Delete();
            //load data
            var success = await GetByNumberAsync(payload.MasterAccountNum, payload.ProfileNum, orderNumber);
            success = success && DeleteData();
            return success;
        }

        /// <summary>
        /// Delete data by number
        /// </summary>
        /// <param name="orderNumber"></param>
        /// <returns></returns>
        public virtual bool DeleteByNumber(PaidbyMapPayload payload, string orderNumber)
        {
            if (string.IsNullOrEmpty(orderNumber))
                return false;
            //set delete mode
            Delete();
            //load data
            var success = GetByNumber(payload.MasterAccountNum, payload.ProfileNum, orderNumber);
            success = success && DeleteData();
            return success;
        }

        public async Task<bool> GetPaidbyMaps(PaidbyMapPayload payload)
        {
            var listSrv = new PaidbyMapList(dbFactory, new PaidbyMapQuery());
            try
            {
                await listSrv.GetPaidbyMapListAsync(payload);
                return true;
            }
            catch (Exception ex)
            {
                payload.ReturnError(ex.Message);
                return false;
            }
        }

        public async Task<bool> UpdatePaidbyMaps(PaidbyMapPayload payload)
        {
            DateTime recentUpdateTime = DateTime.Now;

            int masterAccountNum = payload.MasterAccountNum;
            int profileNum = payload.ProfileNum;
            List<PaidbyMapDataDto> successData = new List<PaidbyMapDataDto>();
            var keys = await PaidbyMapHelper.GetPaidbyMapKeys(masterAccountNum, profileNum);

            foreach (var item in payload.PaidbyMaps)
            {
                var paidbyMap = item.PaidbyMap;
                paidbyMap.MasterAccountNum = masterAccountNum;
                paidbyMap.ProfileNum = profileNum;
                bool exist = keys.Any(k => item.EqualKey(k));

                NewData();

                try
                {
                    if (exist)
                        await UpdateAsync(item);
                    else
                        await AddAsync(item);

                    successData.Add(ToDto());
                    payload.Success &= true;
                }
                catch (Exception ex)
                {
                    string handingItem = JsonConvert.SerializeObject(paidbyMap);
                    payload.ReturnError($"Occured error when saving {Environment.NewLine} {handingItem} {Environment.NewLine} {ex.Message}");
                }
            }
            payload.PaidbyMaps = successData;
            //await PaidbyMapHelper.DeleteObsoleteData(masterAccountNum, profileNum, recentUpdateTime);
            await DeleteObsoleteData(masterAccountNum, profileNum, recentUpdateTime);

            return payload.Success;
        }

        async Task<int> DeleteObsoleteData(int masterAccountNum, int profileNum, DateTime recentUpdateTime)
        {
            var sql = $@"
DELETE FROM PaidbyMap
WHERE MasterAccountNum = @masterAccountNum
AND ProfileNum = @profileNum
AND UpdateDateUtc < @recentUpdateTime
";
            try
            {
                return await dbFactory.Db.ExecuteAsync(sql,
                        masterAccountNum.ToSqlParameter("masterAccountNum"),
                        profileNum.ToSqlParameter("profileNum"),
                        recentUpdateTime.ToSqlParameter("recentUpdateTime")
                    );
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
    }
}



