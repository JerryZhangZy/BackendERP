//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Web;
using System.Threading.Tasks;
using System.Xml.Serialization;
using Newtonsoft.Json;

using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.CommerceCentral.ERPDb;
using Microsoft.AspNetCore.Http;

namespace DigitBridge.CommerceCentral.ERPMdl
{
    /// <summary>
    /// Represents a StyleCodeService.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public class StyleCodeManager :  IStyleCodeManager , IMessage
    {

        public StyleCodeManager() : base() {}

        public StyleCodeManager(IDataBaseFactory dbFactory)
        {
            SetDataBaseFactory(dbFactory);
        }
        
        [XmlIgnore, JsonIgnore]
        protected StyleCodeService _styleCodeService;
        [XmlIgnore, JsonIgnore]
        public StyleCodeService styleCodeService
        {
            get
            {
                if (_styleCodeService is null)
                    _styleCodeService = new StyleCodeService(dbFactory);
                return _styleCodeService;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected StyleCodeDataDtoCsv _styleCodeDataDtoCsv;
        [XmlIgnore, JsonIgnore]
        public StyleCodeDataDtoCsv styleCodeDataDtoCsv
        {
            get
            {
                if (_styleCodeDataDtoCsv is null)
                    _styleCodeDataDtoCsv = new StyleCodeDataDtoCsv();
                return _styleCodeDataDtoCsv;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected StyleCodeList _styleCodeList;
        [XmlIgnore, JsonIgnore]
        public StyleCodeList styleCodeList
        {
            get
            {
                if (_styleCodeList is null)
                    _styleCodeList = new StyleCodeList(dbFactory);
                return _styleCodeList;
            }
        }

        public async Task<byte[]> ExportAsync(StyleCodePayload payload)
        {
            var rowNumList =await styleCodeList.GetRowNumListAsync(payload);
            var dtoList = new List<StyleCodeDataDto>();
           foreach(var x in rowNumList)
            {
                if (styleCodeService.GetData(x))
                    dtoList.Add(styleCodeService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new StyleCodeDataDto());
            return styleCodeDataDtoCsv.Export(dtoList);
        }

        public byte[] Export(StyleCodePayload payload)
        {
            var rowNumList =styleCodeList.GetRowNumList(payload);
            var dtoList = new List<StyleCodeDataDto>();
            foreach (var x in rowNumList)
            {
                if (styleCodeService.GetData(x))
                    dtoList.Add(styleCodeService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new StyleCodeDataDto());
            return styleCodeDataDtoCsv.Export(dtoList);
        }

        public void Import(StyleCodePayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list = styleCodeDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.StyleCode = item;
                    if (styleCodeService.Add(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in styleCodeService.Messages)
                            Messages.Add(msg);
                        styleCodeService.Messages.Clear();
                    }
                }
                if (payload.HasStyleCode)
                    payload.StyleCode = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        public async Task ImportAsync(StyleCodePayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list =styleCodeDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.StyleCode = item;
                    if (await styleCodeService.AddAsync(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in styleCodeService.Messages)
                            Messages.Add(msg);
                        styleCodeService.Messages.Clear();
                    }
                }
                if (payload.HasStyleCode)
                    payload.StyleCode = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        #region DataBase
        [XmlIgnore, JsonIgnore]
        protected IDataBaseFactory _dbFactory;

        [XmlIgnore, JsonIgnore]
        public IDataBaseFactory dbFactory
        {
            get
            {
                if (_dbFactory is null)
                    _dbFactory = DataBaseFactory.CreateDefault();
                return _dbFactory;
            }
        }

        public void SetDataBaseFactory(IDataBaseFactory dbFactory)
        {
            _dbFactory = dbFactory;
        }

        #endregion DataBase

        #region Messages
        protected IList<MessageClass> _messages;
        [XmlIgnore, JsonIgnore]
        public virtual IList<MessageClass> Messages
        {
            get
            {
                if (_messages is null)
                    _messages = new List<MessageClass>();
                return _messages;
            }
            set { _messages = value; }
        }
        public IList<MessageClass> AddInfo(string message, string code = null) =>
             Messages.Add(message, MessageLevel.Info, code);
        public IList<MessageClass> AddWarning(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Warning, code);
        public IList<MessageClass> AddError(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Error, code);
        public IList<MessageClass> AddFatal(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Fatal, code);
        public IList<MessageClass> AddDebug(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Debug, code);

        #endregion Messages
    }
}
