//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Web;
using System.Threading.Tasks;
using System.Xml.Serialization;
using Newtonsoft.Json;

using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.CommerceCentral.ERPDb;
using Microsoft.AspNetCore.Http;

namespace DigitBridge.CommerceCentral.ERPMdl
{
    /// <summary>
    /// Represents a EventProcessERPService.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public class EventProcessERPManager :  IEventProcessERPManager , IMessage
    {

        public EventProcessERPManager() : base() {}

        public EventProcessERPManager(IDataBaseFactory dbFactory)
        {
            SetDataBaseFactory(dbFactory);
        }
        
        [XmlIgnore, JsonIgnore]
        protected EventProcessERPService _eventProcessERPService;
        [XmlIgnore, JsonIgnore]
        public EventProcessERPService eventProcessERPService
        {
            get
            {
                if (_eventProcessERPService is null)
                    _eventProcessERPService = new EventProcessERPService(dbFactory);
                return _eventProcessERPService;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected EventProcessERPDataDtoCsv _eventProcessERPDataDtoCsv;
        [XmlIgnore, JsonIgnore]
        public EventProcessERPDataDtoCsv eventProcessERPDataDtoCsv
        {
            get
            {
                if (_eventProcessERPDataDtoCsv is null)
                    _eventProcessERPDataDtoCsv = new EventProcessERPDataDtoCsv();
                return _eventProcessERPDataDtoCsv;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected EventProcessERPList _eventProcessERPList;
        [XmlIgnore, JsonIgnore]
        public EventProcessERPList eventProcessERPList
        {
            get
            {
                if (_eventProcessERPList is null)
                    _eventProcessERPList = new EventProcessERPList(dbFactory);
                return _eventProcessERPList;
            }
        }

        public async Task<byte[]> ExportAsync(EventProcessERPPayload payload)
        {
            var rowNumList =await eventProcessERPList.GetRowNumListAsync(payload);
            var dtoList = new List<EventProcessERPDataDto>();
           foreach(var x in rowNumList)
            {
                if (eventProcessERPService.GetData(x))
                    dtoList.Add(eventProcessERPService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new EventProcessERPDataDto());
            return eventProcessERPDataDtoCsv.Export(dtoList);
        }

        public byte[] Export(EventProcessERPPayload payload)
        {
            var rowNumList =eventProcessERPList.GetRowNumList(payload);
            var dtoList = new List<EventProcessERPDataDto>();
            foreach (var x in rowNumList)
            {
                if (eventProcessERPService.GetData(x))
                    dtoList.Add(eventProcessERPService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new EventProcessERPDataDto());
            return eventProcessERPDataDtoCsv.Export(dtoList);
        }

        public void Import(EventProcessERPPayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list = eventProcessERPDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.EventProcessERP = item;
                    if (eventProcessERPService.Add(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in eventProcessERPService.Messages)
                            Messages.Add(msg);
                        eventProcessERPService.Messages.Clear();
                    }
                }
                if (payload.HasEventProcessERP)
                    payload.EventProcessERP = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        public async Task ImportAsync(EventProcessERPPayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list =eventProcessERPDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.EventProcessERP = item;
                    if (await eventProcessERPService.AddAsync(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in eventProcessERPService.Messages)
                            Messages.Add(msg);
                        eventProcessERPService.Messages.Clear();
                    }
                }
                if (payload.HasEventProcessERP)
                    payload.EventProcessERP = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        #region DataBase
        [XmlIgnore, JsonIgnore]
        protected IDataBaseFactory _dbFactory;

        [XmlIgnore, JsonIgnore]
        public IDataBaseFactory dbFactory
        {
            get
            {
                if (_dbFactory is null)
                    _dbFactory = DataBaseFactory.CreateDefault();
                return _dbFactory;
            }
        }

        public void SetDataBaseFactory(IDataBaseFactory dbFactory)
        {
            _dbFactory = dbFactory;
        }

        #endregion DataBase

        #region Messages
        protected IList<MessageClass> _messages;
        [XmlIgnore, JsonIgnore]
        public virtual IList<MessageClass> Messages
        {
            get
            {
                if (_messages is null)
                    _messages = new List<MessageClass>();
                return _messages;
            }
            set { _messages = value; }
        }
        public IList<MessageClass> AddInfo(string message, string code = null) =>
             Messages.Add(message, MessageLevel.Info, code);
        public IList<MessageClass> AddWarning(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Warning, code);
        public IList<MessageClass> AddError(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Error, code);
        public IList<MessageClass> AddFatal(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Fatal, code);
        public IList<MessageClass> AddDebug(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Debug, code);

        #endregion Messages
    }
}
