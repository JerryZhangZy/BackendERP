//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Web;
using System.Threading.Tasks;
using System.Xml.Serialization;
using Newtonsoft.Json;

using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.CommerceCentral.ERPDb;
using Microsoft.AspNetCore.Http;

namespace DigitBridge.CommerceCentral.ERPMdl
{
    /// <summary>
    /// Represents a PoTransactionService.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public class PoTransactionManager :  IPoTransactionManager , IMessage
    {

        public PoTransactionManager() : base() {}

        public PoTransactionManager(IDataBaseFactory dbFactory)
        {
            SetDataBaseFactory(dbFactory);
        }
        
        [XmlIgnore, JsonIgnore]
        protected PoTransactionService _poTransactionService;
        [XmlIgnore, JsonIgnore]
        public PoTransactionService poTransactionService
        {
            get
            {
                if (_poTransactionService is null)
                    _poTransactionService = new PoTransactionService(dbFactory);
                return _poTransactionService;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected PoTransactionDataDtoCsv _poTransactionDataDtoCsv;
        [XmlIgnore, JsonIgnore]
        public PoTransactionDataDtoCsv poTransactionDataDtoCsv
        {
            get
            {
                if (_poTransactionDataDtoCsv is null)
                    _poTransactionDataDtoCsv = new PoTransactionDataDtoCsv();
                return _poTransactionDataDtoCsv;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected PoTransactionList _poTransactionList;
        [XmlIgnore, JsonIgnore]
        public PoTransactionList poTransactionList
        {
            get
            {
                if (_poTransactionList is null)
                    _poTransactionList = new PoTransactionList(dbFactory);
                return _poTransactionList;
            }
        }

        public async Task<byte[]> ExportAsync(PoTransactionPayload payload)
        {
            var rowNumList =await poTransactionList.GetRowNumListAsync(payload);
            var dtoList = new List<PoTransactionDataDto>();
           foreach(var x in rowNumList)
            {
                if (poTransactionService.GetData(x))
                    dtoList.Add(poTransactionService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new PoTransactionDataDto());
            return poTransactionDataDtoCsv.Export(dtoList);
        }

        public byte[] Export(PoTransactionPayload payload)
        {
            var rowNumList =poTransactionList.GetRowNumList(payload);
            var dtoList = new List<PoTransactionDataDto>();
            foreach (var x in rowNumList)
            {
                if (poTransactionService.GetData(x))
                    dtoList.Add(poTransactionService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new PoTransactionDataDto());
            return poTransactionDataDtoCsv.Export(dtoList);
        }

        public void Import(PoTransactionPayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list = poTransactionDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.PoTransaction = item;
                    if (poTransactionService.Add(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in poTransactionService.Messages)
                            Messages.Add(msg);
                        poTransactionService.Messages.Clear();
                    }
                }
                if (payload.HasPoTransaction)
                    payload.PoTransaction = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        public async Task ImportAsync(PoTransactionPayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list =poTransactionDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.PoTransaction = item;
                    if (await poTransactionService.AddAsync(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in poTransactionService.Messages)
                            Messages.Add(msg);
                        poTransactionService.Messages.Clear();
                    }
                }
                if (payload.HasPoTransaction)
                    payload.PoTransaction = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        #region DataBase
        [XmlIgnore, JsonIgnore]
        protected IDataBaseFactory _dbFactory;

        [XmlIgnore, JsonIgnore]
        public IDataBaseFactory dbFactory
        {
            get
            {
                if (_dbFactory is null)
                    _dbFactory = DataBaseFactory.CreateDefault();
                return _dbFactory;
            }
        }

        public void SetDataBaseFactory(IDataBaseFactory dbFactory)
        {
            _dbFactory = dbFactory;
        }

        #endregion DataBase

        #region Messages
        protected IList<MessageClass> _messages;
        [XmlIgnore, JsonIgnore]
        public virtual IList<MessageClass> Messages
        {
            get
            {
                if (_messages is null)
                    _messages = new List<MessageClass>();
                return _messages;
            }
            set { _messages = value; }
        }
        public IList<MessageClass> AddInfo(string message, string code = null) =>
             Messages.Add(message, MessageLevel.Info, code);
        public IList<MessageClass> AddWarning(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Warning, code);
        public IList<MessageClass> AddError(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Error, code);
        public IList<MessageClass> AddFatal(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Fatal, code);
        public IList<MessageClass> AddDebug(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Debug, code);

        #endregion Messages
    }
}
