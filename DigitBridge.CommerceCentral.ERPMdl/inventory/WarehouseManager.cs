

              
    

//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Web;
using System.Threading.Tasks;
using System.Xml.Serialization;
using Newtonsoft.Json;

using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.CommerceCentral.ERPDb;
using Microsoft.AspNetCore.Http;

namespace DigitBridge.CommerceCentral.ERPMdl
{
    /// <summary>
    /// Represents a SalesOrderService.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public class WarehouseManager : IMessage
    {
        protected WarehouseService warehouseService;
        protected WarehouseDataDtoCsv warehouseDataDtoCsv;
        protected WarehouseList warehouseList;

        public WarehouseManager() : base() {}
        public WarehouseManager(IDataBaseFactory dbFactory)
        {
            SetDataBaseFactory(dbFactory);
            warehouseService = new WarehouseService(dbFactory);
            warehouseDataDtoCsv = new WarehouseDataDtoCsv();
            warehouseList = new WarehouseList(dbFactory);
        }

        public async Task<byte[]> ExportAsync(WarehousePayload payload)
        {
            var rowNumList =await warehouseList.GetRowNumListAsync(payload);
            var dtoList = new List<WarehouseDataDto>();
           foreach(var x in rowNumList)
            {
                if (warehouseService.GetData(x))
                    dtoList.Add(warehouseService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new WarehouseDataDto());
            return warehouseDataDtoCsv.Export(dtoList);
        }

        public byte[] Export(WarehousePayload payload)
        {
            var rowNumList = warehouseList.GetRowNumList(payload);
            var dtoList = new List<WarehouseDataDto>();
            foreach (var x in rowNumList)
            {
                if (warehouseService.GetData(x))
                    dtoList.Add(warehouseService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new WarehouseDataDto());
            return warehouseDataDtoCsv.Export(dtoList);
        }

        public void Import(WarehousePayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list = warehouseDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.Warehouse = item;
                    if (warehouseService.Add(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in warehouseService.Messages)
                            Messages.Add(msg);
                        warehouseService.Messages.Clear();
                    }
                }
                if (payload.HasWarehouse)
                    payload.Warehouse = null;
                AddInfo($"FIle:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        public async Task ImportAsync(WarehousePayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list = warehouseDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.Warehouse = item;
                    if (await warehouseService.AddAsync(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in warehouseService.Messages)
                            Messages.Add(msg);
                        warehouseService.Messages.Clear();
                    }
                }
                if (payload.HasWarehouse)
                    payload.Warehouse = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        #region DataBase
        [XmlIgnore, JsonIgnore]
        protected IDataBaseFactory _dbFactory;

        [XmlIgnore, JsonIgnore]
        public IDataBaseFactory dbFactory
        {
            get
            {
                if (_dbFactory is null)
                    _dbFactory = DataBaseFactory.CreateDefault();
                return _dbFactory;
            }
        }

        public void SetDataBaseFactory(IDataBaseFactory dbFactory)
        {
            _dbFactory = dbFactory;
        }

        #endregion DataBase

        #region Messages
        protected IList<MessageClass> _messages;
        [XmlIgnore, JsonIgnore]
        public virtual IList<MessageClass> Messages
        {
            get
            {
                if (_messages is null)
                    _messages = new List<MessageClass>();
                return _messages;
            }
            set { _messages = value; }
        }
        public IList<MessageClass> AddInfo(string message, string code = null) =>
             Messages.Add(message, MessageLevel.Info, code);
        public IList<MessageClass> AddWarning(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Warning, code);
        public IList<MessageClass> AddError(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Error, code);
        public IList<MessageClass> AddFatal(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Fatal, code);
        public IList<MessageClass> AddDebug(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Debug, code);

        #endregion Messages


    }
}



