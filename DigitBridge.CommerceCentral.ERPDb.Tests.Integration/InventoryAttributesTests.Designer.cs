              
    

//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;
using System.Threading.Tasks;
using System.Data;
using Microsoft.Extensions.Configuration;
using Microsoft.Data.SqlClient;
using Xunit;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.XUnit.Common;
using Bogus;

namespace DigitBridge.CommerceCentral.ERPDb.Tests.Integration
{
    /// <summary>
    /// Represents a Tester for InventoryAttributes.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public partial class InventoryAttributesTests : IDisposable, IClassFixture<TestFixture<StartupTest>>
    {
        public static Faker<InventoryAttributes> GetFakerData()
        {
            #region faker data rules
            return new Faker<InventoryAttributes>()
					.RuleFor(u => u.ProductUuid, f => f.Random.Guid().ToString())
					.RuleFor(u => u.InventoryUuid, f => f.Random.Guid().ToString())
					.RuleFor(u => u.Fields, (f, u) => u.Fields.SetValues(f.Random.JObject()))
					;
            #endregion faker data rules
        }

        protected const string SkipReason = "Debug Helper Function";

        protected TestFixture<StartupTest> Fixture { get; }
        public IConfiguration Configuration { get; }
        public Faker<InventoryAttributes> FakerData { get; set; }
        public IDataBaseFactory DataBaseFactory { get; set; }

        public InventoryAttributesTests(TestFixture<StartupTest> fixture) 
        {
            Fixture = fixture;
            Configuration = fixture.Configuration;

            InitForTest();
        }
        protected void InitForTest()
        {
            var Seq = 0;
            DataBaseFactory = new DataBaseFactory(Configuration["dsn"]);
            FakerData = GetFakerData();
        }
        public void Dispose()
        {
        }

        //[Fact()]
        [Fact(Skip = SkipReason)]
        public void Register_Test()
        {
            var data = FakerData.Generate();
            data.Register();
            var poco = data.GetPocoData();

            Assert.True(true, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void CopyFrom_Test()
        {
            var data = FakerData.Generate();
            var dataJson = data.ObjectToString();
            var newData = FakerData.Generate();
            var newDataJson = newData.ObjectToString();
            data?.CopyFrom(newData);
            var result = !data.Equals(newData);

			Assert.True(true, "This is a generated tester, please report any tester bug to team leader.");
        }

        #region sync methods

        [Fact()]
		//[Fact(Skip = SkipReason)]
		public void Add_Test()
		{
			var data = FakerData.Generate();
            data.SetDataBaseFactory(DataBaseFactory);
            DataBaseFactory.Begin();
			data.Add();
            DataBaseFactory.Commit();

            var dataGet = DataBaseFactory.GetFromCacheById<InventoryAttributes>(data.UniqueId);
            var result = data.Equals(dataGet);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Put_Test()
        {
            var list = DataBaseFactory.Find<InventoryAttributes>("SELECT TOP 1 * FROM InventoryAttributes").ToList();

            DataBaseFactory.Begin();
            var data = list.FirstOrDefault();
            data.SetDataBaseFactory(DataBaseFactory);
            var newData = FakerData.Generate();
            data?.CopyFrom(newData);
            data.Put();
            DataBaseFactory.Commit();

            var dataGet = DataBaseFactory.GetFromCacheById<InventoryAttributes>(data.UniqueId);
            var result = data.Equals(dataGet);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Patch_Test()
        {
            var list = DataBaseFactory.Find<InventoryAttributes>("SELECT TOP 1 * FROM InventoryAttributes").ToList();

            DataBaseFactory.Begin();
            var data = list.FirstOrDefault();
            var dataOrig = new InventoryAttributes();
            dataOrig?.CopyFrom(data);

            data.SetDataBaseFactory(DataBaseFactory);
            var newData = FakerData.Generate();
            data?.CopyFrom(newData);
            data.Patch(new[] { "JsonFields", "JsonFields" });
            DataBaseFactory.Commit();

            var dataGet = DataBaseFactory.GetFromCache<InventoryAttributes>(data.RowNum);
            var result = dataGet.JsonFields != dataOrig.JsonFields &&
                            dataGet.JsonFields != dataOrig.JsonFields &&
                            dataGet.JsonFields == newData.JsonFields &&
                            dataGet.JsonFields == newData.JsonFields;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Save_Test()
        {
	        var dataNew = FakerData.Generate();
            dataNew.SetDataBaseFactory(DataBaseFactory);
            DataBaseFactory.Begin();
            dataNew.Save();
            DataBaseFactory.Commit();

            var dataUpdate = DataBaseFactory.GetById<InventoryAttributes>(dataNew.UniqueId);
			var dataChanged = FakerData.Generate();
            dataUpdate?.CopyFrom(dataChanged, new[] {"InventoryUuid"});

            DataBaseFactory.Begin();
            dataUpdate.Save();
            DataBaseFactory.Commit();

            var dataGet = DataBaseFactory.GetFromCacheById<InventoryAttributes>(dataUpdate.UniqueId);
            var result = dataUpdate.Equals(dataGet);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Delete_Test()
        { 
            var list = DataBaseFactory.Find<InventoryAttributes>("SELECT TOP 1 * FROM InventoryAttributes").ToList();
            var data = list.FirstOrDefault();

            DataBaseFactory.Begin();
            data.SetDataBaseFactory(DataBaseFactory);
            data.Delete();
            DataBaseFactory.Commit();

            var result = DataBaseFactory.ExistUniqueId<InventoryAttributes>(data.UniqueId);

            Assert.True(!result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Get_Test()
        {
            //var list = DataBaseFactory.Find<InventoryAttributes>().ToList();
            //var listData = list.FirstOrDefault();
            //var data = DataBaseFactory.Get<InventoryAttributes>(listData.RowNum);
            //var result = data.Equals(listData);

            var list = DataBaseFactory.Find<InventoryAttributes>("SELECT TOP 1 * FROM InventoryAttributes").ToList();
            var listData = list.FirstOrDefault(); 
            var result = listData!=null;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void GetById_Test()
        {
            var list = DataBaseFactory.Find<InventoryAttributes>("SELECT TOP 1 * FROM InventoryAttributes").ToList();
            var listData = list.FirstOrDefault();
            var data = DataBaseFactory.GetById<InventoryAttributes>(listData.UniqueId);
            var result = data.Equals(listData);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void AddList_Test()
        {
            var list = FakerData.Generate(10);
            var ProductUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.ProductUuid = ProductUuid);
            list.SetDataBaseFactory<InventoryAttributes>(DataBaseFactory)
                .Save<InventoryAttributes>();

            var cnt = DataBaseFactory.Count<InventoryAttributes>("WHERE ProductUuid = @0", ProductUuid);
            var result = cnt.Equals(list.Count());

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void SaveList_Test()
        {
            var list = FakerData.Generate(10);
            var ProductUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.ProductUuid = ProductUuid);
            list.SetDataBaseFactory<InventoryAttributes>(DataBaseFactory)
                .Save<InventoryAttributes>();

            var NewJsonFields = Guid.NewGuid().ToString();
            var listFind = DataBaseFactory.Find<InventoryAttributes>("WHERE ProductUuid = @0 ORDER BY RowNum", ProductUuid).ToList();
            listFind.ToList().ForEach(x => x.JsonFields = NewJsonFields);
            listFind.Save<InventoryAttributes>();

            list = DataBaseFactory.Find<InventoryAttributes>("WHERE ProductUuid = @0 ORDER BY RowNum", ProductUuid).ToList();
            var result = list.Where(x => x.JsonFields == NewJsonFields).Count() == listFind.Count();

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void DeleteList_Test()
        {
            var list = FakerData.Generate(10);
            var ProductUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.ProductUuid = ProductUuid);
            list.SetDataBaseFactory<InventoryAttributes>(DataBaseFactory)
                .Save();

            var listFind = DataBaseFactory.Find<InventoryAttributes>("WHERE ProductUuid = @0 ORDER BY RowNum", ProductUuid).ToList();
            listFind.Delete();

            var cnt = DataBaseFactory.Count<InventoryAttributes>("WHERE ProductUuid = @0", ProductUuid);
            var result = cnt == 0;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void GetFromCacheById_Test()
        {
            var list = DataBaseFactory.Find<InventoryAttributes>("SELECT TOP 1 * FROM InventoryAttributes").ToList();
            var data = list.FirstOrDefault();
            var data1 = DataBaseFactory.GetFromCacheById<InventoryAttributes>(data.UniqueId);
            var data2 = DataBaseFactory.GetFromCacheById<InventoryAttributes>(data.UniqueId);

            var result = data1 == data2;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        #endregion sync methods

        #region async methods

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task AddAsync_Test()
        {
            var data = FakerData.Generate();
            data.SetDataBaseFactory(DataBaseFactory);
            DataBaseFactory.Begin();
            await data.AddAsync();
            DataBaseFactory.Commit();

            var dataGet = await DataBaseFactory.GetFromCacheByIdAsync<InventoryAttributes>(data.UniqueId);
            var result = data.Equals(dataGet);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task PutAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InventoryAttributes>()).ToList();

            DataBaseFactory.Begin();
            var data = list.FirstOrDefault();
            data.SetDataBaseFactory(DataBaseFactory);
            var newData = FakerData.Generate();
            data?.CopyFrom(newData);
            await data.PutAsync();
            DataBaseFactory.Commit();

            var dataGet = await DataBaseFactory.GetFromCacheByIdAsync<InventoryAttributes>(data.UniqueId);
            var result = data.Equals(dataGet);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task PatchAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InventoryAttributes>()).ToList();

            DataBaseFactory.Begin();
            var data = list.FirstOrDefault();
            var dataOrig = new InventoryAttributes();
            dataOrig?.CopyFrom(data);

            data.SetDataBaseFactory(DataBaseFactory);
            var newData = FakerData.Generate();
            data?.CopyFrom(newData);
            await data.PatchAsync(new[] { "JsonFields", "JsonFields" });
            DataBaseFactory.Commit();

            var dataGet = await DataBaseFactory.GetFromCacheAsync<InventoryAttributes>(data.RowNum);
            var result = dataGet.JsonFields != dataOrig.JsonFields &&
                            dataGet.JsonFields != dataOrig.JsonFields &&
                            dataGet.JsonFields == newData.JsonFields &&
                            dataGet.JsonFields == newData.JsonFields;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task SaveAsync_Test()
        {
            var dataNew = FakerData.Generate();
            dataNew.SetDataBaseFactory(DataBaseFactory);
            DataBaseFactory.Begin();
            await dataNew.SaveAsync();
            DataBaseFactory.Commit();

            var dataUpdate = await DataBaseFactory.GetByIdAsync<InventoryAttributes>(dataNew.UniqueId);
            var dataChanged = FakerData.Generate();
            dataUpdate?.CopyFrom(dataChanged, new[] { "InventoryUuid" });

            DataBaseFactory.Begin();
            await dataUpdate.SaveAsync();
            DataBaseFactory.Commit();

            var dataGet = await DataBaseFactory.GetFromCacheByIdAsync<InventoryAttributes>(dataUpdate.UniqueId);
            var result = dataUpdate.Equals(dataGet);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task DeleteAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InventoryAttributes>()).ToList();
            var data = list.FirstOrDefault();

            DataBaseFactory.Begin();
            data.SetDataBaseFactory(DataBaseFactory);
            await data.DeleteAsync();
            DataBaseFactory.Commit();

            var result = await DataBaseFactory.ExistUniqueIdAsync<InventoryAttributes>(data.UniqueId);

            Assert.True(!result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task GetAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InventoryAttributes>()).ToList();
            var listData = list.FirstOrDefault();
            var data = await DataBaseFactory.GetAsync<InventoryAttributes>(listData.RowNum);
            var result = data.Equals(listData);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task GetByIdAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InventoryAttributes>()).ToList();
            var listData = list.FirstOrDefault();
            var data = await DataBaseFactory.GetByIdAsync<InventoryAttributes>(listData.UniqueId);
            var result = data.Equals(listData);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task AddListAsync_Test()
        {
            var list = FakerData.Generate(10);
            var ProductUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.ProductUuid = ProductUuid);
            await list
                .SetDataBaseFactory<InventoryAttributes>(DataBaseFactory)
                .SaveAsync<InventoryAttributes>();

            var cnt = await DataBaseFactory.CountAsync<InventoryAttributes>("WHERE ProductUuid = @0", ProductUuid);
            var result = cnt.Equals(list.Count());

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task SaveListAsync_Test()
        {
            var list = FakerData.Generate(10);
            var ProductUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.ProductUuid = ProductUuid);
            await list
                .SetDataBaseFactory<InventoryAttributes>(DataBaseFactory)
                .SaveAsync<InventoryAttributes>();

            var NewJsonFields = Guid.NewGuid().ToString();
            var listFind = (await DataBaseFactory.FindAsync<InventoryAttributes>("WHERE ProductUuid = @0 ORDER BY RowNum", ProductUuid)).ToList();
            listFind.ToList().ForEach(x => x.JsonFields = NewJsonFields);
            await listFind.SaveAsync<InventoryAttributes>();

            list = DataBaseFactory.Find<InventoryAttributes>("WHERE ProductUuid = @0 ORDER BY RowNum", ProductUuid).ToList();
            var result = list.Where(x => x.JsonFields == NewJsonFields).Count() == listFind.Count();

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task DeleteListAsync_Test()
        {
            var list = FakerData.Generate(10);
            var ProductUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.ProductUuid = ProductUuid);
            await list
                .SetDataBaseFactory<InventoryAttributes>(DataBaseFactory)
                .SaveAsync();

            var listFind = (await DataBaseFactory.FindAsync<InventoryAttributes>("WHERE ProductUuid = @0 ORDER BY RowNum", ProductUuid)).ToList();
            await listFind.DeleteAsync();

            var cnt = await DataBaseFactory.CountAsync<InventoryAttributes>("WHERE ProductUuid = @0", ProductUuid);
            var result = cnt == 0;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task GetFromCacheByIdAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InventoryAttributes>("SELECT TOP 1 * FROM InventoryAttributes")).ToList();
            var data = list.FirstOrDefault();
            var data1 = await DataBaseFactory.GetFromCacheByIdAsync<InventoryAttributes>(data.UniqueId);
            var data2 = await DataBaseFactory.GetFromCacheByIdAsync<InventoryAttributes>(data.UniqueId);

            var result = data1 == data2;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        #endregion sync methods

    }
}


