              
    

//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;
using System.Threading.Tasks;
using System.Data;
using Microsoft.Extensions.Configuration;
using Microsoft.Data.SqlClient;
using Xunit;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.XUnit.Common;
using Bogus;

namespace DigitBridge.CommerceCentral.ERPDb.Tests.Integration
{
    /// <summary>
    /// Represents a Tester for InitNumbers.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public partial class InitNumbersTests : IDisposable, IClassFixture<TestFixture<StartupTest>>
    {
        public static Faker<InitNumbers> GetFakerData()
        {
            #region faker data rules
            return new Faker<InitNumbers>()
					.RuleFor(u => u.DatabaseNum, f => f.Random.Int(1, 100))
					.RuleFor(u => u.MasterAccountNum, f => f.Random.Int(1, 100))
					.RuleFor(u => u.ProfileNum, f => f.Random.Int(1, 100))
					.RuleFor(u => u.InitNumbersUuid, f => f.Random.Guid().ToString())
					.RuleFor(u => u.CustomerUuid, f => f.Random.Guid().ToString())
					.RuleFor(u => u.InActive, f => f.Random.Bool())
					.RuleFor(u => u.Type, f => f.Lorem.Sentence().TruncateTo(20))
					.RuleFor(u => u.CurrentNumber, f => f.Random.Int(1, 100))
					.RuleFor(u => u.Number, f => default(long))
					.RuleFor(u => u.MaxNumber, f => default(long))
					.RuleFor(u => u.EndNumber, f => default(long))
					.RuleFor(u => u.Prefix, f => f.Lorem.Sentence().TruncateTo(20))
					.RuleFor(u => u.Suffix, f => f.Lorem.Sentence().TruncateTo(20))
					.RuleFor(u => u.UpdateDateUtc, f => f.Date.Past(0).Date)
					.RuleFor(u => u.EnterBy, f => f.Lorem.Sentence().TruncateTo(100))
					.RuleFor(u => u.UpdateBy, f => f.Lorem.Sentence().TruncateTo(100))
					;
            #endregion faker data rules
        }

        protected const string SkipReason = "Debug Helper Function";

        protected TestFixture<StartupTest> Fixture { get; }
        public IConfiguration Configuration { get; }
        public Faker<InitNumbers> FakerData { get; set; }
        public IDataBaseFactory DataBaseFactory { get; set; }

        public InitNumbersTests(TestFixture<StartupTest> fixture) 
        {
            Fixture = fixture;
            Configuration = fixture.Configuration;

            InitForTest();
        }
        protected void InitForTest()
        {
            var Seq = 0;
            DataBaseFactory = new DataBaseFactory(Configuration["dsn"]);
            FakerData = GetFakerData();
        }
        public void Dispose()
        {
        }

        //[Fact()]
        [Fact(Skip = SkipReason)]
        public void Register_Test()
        {
            var data = FakerData.Generate();
            data.Register();
            var poco = data.GetPocoData();

            Assert.True(true, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void CopyFrom_Test()
        {
            var data = FakerData.Generate();
            var dataJson = data.ObjectToString();
            var newData = FakerData.Generate();
            var newDataJson = newData.ObjectToString();
            data?.CopyFrom(newData);
            var result = !data.Equals(newData);

			Assert.True(true, "This is a generated tester, please report any tester bug to team leader.");
        }

        #region sync methods

        [Fact()]
		//[Fact(Skip = SkipReason)]
		public void Add_Test()
		{
			var data = FakerData.Generate();
            data.SetDataBaseFactory(DataBaseFactory);
            DataBaseFactory.Begin();
			data.Add();
            DataBaseFactory.Commit();

            var dataGet = DataBaseFactory.GetFromCacheById<InitNumbers>(data.UniqueId);
            var result = data.Equals(dataGet);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Put_Test()
        {
            var list = DataBaseFactory.Find<InitNumbers>("SELECT TOP 1 * FROM InitNumbers").ToList();

            DataBaseFactory.Begin();
            var data = list.FirstOrDefault();
            data.SetDataBaseFactory(DataBaseFactory);
            var newData = FakerData.Generate();
            data?.CopyFrom(newData);
            data.Put();
            DataBaseFactory.Commit();

            var dataGet = DataBaseFactory.GetFromCacheById<InitNumbers>(data.UniqueId);
            var result = data.Equals(dataGet);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Patch_Test()
        {
            var list = DataBaseFactory.Find<InitNumbers>("SELECT TOP 1 * FROM InitNumbers").ToList();

            DataBaseFactory.Begin();
            var data = list.FirstOrDefault();
            var dataOrig = new InitNumbers();
            dataOrig?.CopyFrom(data);

            data.SetDataBaseFactory(DataBaseFactory);
            var newData = FakerData.Generate();
            data?.CopyFrom(newData);
            data.Patch(new[] { "Type", "Prefix" });
            DataBaseFactory.Commit();

            var dataGet = DataBaseFactory.GetFromCache<InitNumbers>(data.RowNum);
            var result = dataGet.Type != dataOrig.Type &&
                            dataGet.Prefix != dataOrig.Prefix &&
                            dataGet.Type == newData.Type &&
                            dataGet.Prefix == newData.Prefix;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Save_Test()
        {
	        var dataNew = FakerData.Generate();
            dataNew.SetDataBaseFactory(DataBaseFactory);
            DataBaseFactory.Begin();
            dataNew.Save();
            DataBaseFactory.Commit();

            var dataUpdate = DataBaseFactory.GetById<InitNumbers>(dataNew.UniqueId);
			var dataChanged = FakerData.Generate();
            dataUpdate?.CopyFrom(dataChanged, new[] {"InitNumbersUuid"});

            DataBaseFactory.Begin();
            dataUpdate.Save();
            DataBaseFactory.Commit();

            var dataGet = DataBaseFactory.GetFromCacheById<InitNumbers>(dataUpdate.UniqueId);
            var result = dataUpdate.Equals(dataGet);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Delete_Test()
        { 
            var list = DataBaseFactory.Find<InitNumbers>("SELECT TOP 1 * FROM InitNumbers").ToList();
            var data = list.FirstOrDefault();

            DataBaseFactory.Begin();
            data.SetDataBaseFactory(DataBaseFactory);
            data.Delete();
            DataBaseFactory.Commit();

            var result = DataBaseFactory.ExistUniqueId<InitNumbers>(data.UniqueId);

            Assert.True(!result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Get_Test()
        {
            //var list = DataBaseFactory.Find<InitNumbers>().ToList();
            //var listData = list.FirstOrDefault();
            //var data = DataBaseFactory.Get<InitNumbers>(listData.RowNum);
            //var result = data.Equals(listData);

            var list = DataBaseFactory.Find<InitNumbers>("SELECT TOP 1 * FROM InitNumbers").ToList();
            var listData = list.FirstOrDefault(); 
            var result = listData!=null;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void GetById_Test()
        {
            var list = DataBaseFactory.Find<InitNumbers>("SELECT TOP 1 * FROM InitNumbers").ToList();
            var listData = list.FirstOrDefault();
            var data = DataBaseFactory.GetById<InitNumbers>(listData.UniqueId);
            var result = data.Equals(listData);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void AddList_Test()
        {
            var list = FakerData.Generate(10);
            var CustomerUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.CustomerUuid = CustomerUuid);
            list.SetDataBaseFactory<InitNumbers>(DataBaseFactory)
                .Save<InitNumbers>();

            var cnt = DataBaseFactory.Count<InitNumbers>("WHERE CustomerUuid = @0", CustomerUuid);
            var result = cnt.Equals(list.Count());

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void SaveList_Test()
        {
            var list = FakerData.Generate(10);
            var CustomerUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.CustomerUuid = CustomerUuid);
            list.SetDataBaseFactory<InitNumbers>(DataBaseFactory)
                .Save<InitNumbers>();

            var NewPrefix = Guid.NewGuid().ToString();
            var listFind = DataBaseFactory.Find<InitNumbers>("WHERE CustomerUuid = @0 ORDER BY RowNum", CustomerUuid).ToList();
            listFind.ToList().ForEach(x => x.Prefix = NewPrefix);
            listFind.Save<InitNumbers>();

            list = DataBaseFactory.Find<InitNumbers>("WHERE CustomerUuid = @0 ORDER BY RowNum", CustomerUuid).ToList();
            var result = list.Where(x => x.Prefix == NewPrefix).Count() == listFind.Count();

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void DeleteList_Test()
        {
            var list = FakerData.Generate(10);
            var CustomerUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.CustomerUuid = CustomerUuid);
            list.SetDataBaseFactory<InitNumbers>(DataBaseFactory)
                .Save();

            var listFind = DataBaseFactory.Find<InitNumbers>("WHERE CustomerUuid = @0 ORDER BY RowNum", CustomerUuid).ToList();
            listFind.Delete();

            var cnt = DataBaseFactory.Count<InitNumbers>("WHERE CustomerUuid = @0", CustomerUuid);
            var result = cnt == 0;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void GetFromCacheById_Test()
        {
            var list = DataBaseFactory.Find<InitNumbers>("SELECT TOP 1 * FROM InitNumbers").ToList();
            var data = list.FirstOrDefault();
            var data1 = DataBaseFactory.GetFromCacheById<InitNumbers>(data.UniqueId);
            var data2 = DataBaseFactory.GetFromCacheById<InitNumbers>(data.UniqueId);

            var result = data1 == data2;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        #endregion sync methods

        #region async methods

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task AddAsync_Test()
        {
            var data = FakerData.Generate();
            data.SetDataBaseFactory(DataBaseFactory);
            DataBaseFactory.Begin();
            await data.AddAsync();
            DataBaseFactory.Commit();

            var dataGet = await DataBaseFactory.GetFromCacheByIdAsync<InitNumbers>(data.UniqueId);
            var result = data.Equals(dataGet);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task PutAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InitNumbers>()).ToList();

            DataBaseFactory.Begin();
            var data = list.FirstOrDefault();
            data.SetDataBaseFactory(DataBaseFactory);
            var newData = FakerData.Generate();
            data?.CopyFrom(newData);
            await data.PutAsync();
            DataBaseFactory.Commit();

            var dataGet = await DataBaseFactory.GetFromCacheByIdAsync<InitNumbers>(data.UniqueId);
            var result = data.Equals(dataGet);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task PatchAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InitNumbers>()).ToList();

            DataBaseFactory.Begin();
            var data = list.FirstOrDefault();
            var dataOrig = new InitNumbers();
            dataOrig?.CopyFrom(data);

            data.SetDataBaseFactory(DataBaseFactory);
            var newData = FakerData.Generate();
            data?.CopyFrom(newData);
            await data.PatchAsync(new[] { "Type", "Prefix" });
            DataBaseFactory.Commit();

            var dataGet = await DataBaseFactory.GetFromCacheAsync<InitNumbers>(data.RowNum);
            var result = dataGet.Type != dataOrig.Type &&
                            dataGet.Prefix != dataOrig.Prefix &&
                            dataGet.Type == newData.Type &&
                            dataGet.Prefix == newData.Prefix;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task SaveAsync_Test()
        {
            var dataNew = FakerData.Generate();
            dataNew.SetDataBaseFactory(DataBaseFactory);
            DataBaseFactory.Begin();
            await dataNew.SaveAsync();
            DataBaseFactory.Commit();

            var dataUpdate = await DataBaseFactory.GetByIdAsync<InitNumbers>(dataNew.UniqueId);
            var dataChanged = FakerData.Generate();
            dataUpdate?.CopyFrom(dataChanged, new[] { "InitNumbersUuid" });

            DataBaseFactory.Begin();
            await dataUpdate.SaveAsync();
            DataBaseFactory.Commit();

            var dataGet = await DataBaseFactory.GetFromCacheByIdAsync<InitNumbers>(dataUpdate.UniqueId);
            var result = dataUpdate.Equals(dataGet);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task DeleteAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InitNumbers>()).ToList();
            var data = list.FirstOrDefault();

            DataBaseFactory.Begin();
            data.SetDataBaseFactory(DataBaseFactory);
            await data.DeleteAsync();
            DataBaseFactory.Commit();

            var result = await DataBaseFactory.ExistUniqueIdAsync<InitNumbers>(data.UniqueId);

            Assert.True(!result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task GetAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InitNumbers>()).ToList();
            var listData = list.FirstOrDefault();
            var data = await DataBaseFactory.GetAsync<InitNumbers>(listData.RowNum);
            var result = data.Equals(listData);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task GetByIdAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InitNumbers>()).ToList();
            var listData = list.FirstOrDefault();
            var data = await DataBaseFactory.GetByIdAsync<InitNumbers>(listData.UniqueId);
            var result = data.Equals(listData);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task AddListAsync_Test()
        {
            var list = FakerData.Generate(10);
            var CustomerUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.CustomerUuid = CustomerUuid);
            await list
                .SetDataBaseFactory<InitNumbers>(DataBaseFactory)
                .SaveAsync<InitNumbers>();

            var cnt = await DataBaseFactory.CountAsync<InitNumbers>("WHERE CustomerUuid = @0", CustomerUuid);
            var result = cnt.Equals(list.Count());

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task SaveListAsync_Test()
        {
            var list = FakerData.Generate(10);
            var CustomerUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.CustomerUuid = CustomerUuid);
            await list
                .SetDataBaseFactory<InitNumbers>(DataBaseFactory)
                .SaveAsync<InitNumbers>();

            var NewPrefix = Guid.NewGuid().ToString();
            var listFind = (await DataBaseFactory.FindAsync<InitNumbers>("WHERE CustomerUuid = @0 ORDER BY RowNum", CustomerUuid)).ToList();
            listFind.ToList().ForEach(x => x.Prefix = NewPrefix);
            await listFind.SaveAsync<InitNumbers>();

            list = DataBaseFactory.Find<InitNumbers>("WHERE CustomerUuid = @0 ORDER BY RowNum", CustomerUuid).ToList();
            var result = list.Where(x => x.Prefix == NewPrefix).Count() == listFind.Count();

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task DeleteListAsync_Test()
        {
            var list = FakerData.Generate(10);
            var CustomerUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.CustomerUuid = CustomerUuid);
            await list
                .SetDataBaseFactory<InitNumbers>(DataBaseFactory)
                .SaveAsync();

            var listFind = (await DataBaseFactory.FindAsync<InitNumbers>("WHERE CustomerUuid = @0 ORDER BY RowNum", CustomerUuid)).ToList();
            await listFind.DeleteAsync();

            var cnt = await DataBaseFactory.CountAsync<InitNumbers>("WHERE CustomerUuid = @0", CustomerUuid);
            var result = cnt == 0;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task GetFromCacheByIdAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InitNumbers>("SELECT TOP 1 * FROM InitNumbers")).ToList();
            var data = list.FirstOrDefault();
            var data1 = await DataBaseFactory.GetFromCacheByIdAsync<InitNumbers>(data.UniqueId);
            var data2 = await DataBaseFactory.GetFromCacheByIdAsync<InitNumbers>(data.UniqueId);

            var result = data1 == data2;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        #endregion sync methods

    }
}


