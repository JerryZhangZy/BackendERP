              
    

//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;
using System.Threading.Tasks;
using System.Data;
using Microsoft.Extensions.Configuration;
using Microsoft.Data.SqlClient;
using Xunit;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.XUnit.Common;
using Bogus;

namespace DigitBridge.CommerceCentral.ERPDb.Tests.Integration
{
    /// <summary>
    /// Represents a Tester for InventoryUpdateHeader.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public partial class InventoryUpdateHeaderTests : IDisposable, IClassFixture<TestFixture<StartupTest>>
    {
        public static Faker<InventoryUpdateHeader> GetFakerData()
        {
            #region faker data rules
            return new Faker<InventoryUpdateHeader>()
					.RuleFor(u => u.DatabaseNum, f => f.Random.Int(1, 100))
					.RuleFor(u => u.MasterAccountNum, f => f.Random.Int(1, 100))
					.RuleFor(u => u.ProfileNum, f => f.Random.Int(1, 100))
					.RuleFor(u => u.InventoryUpdateUuid, f => f.Random.Guid().ToString())
					.RuleFor(u => u.BatchNumber, f => f.Random.AlphaNumeric(50))
					.RuleFor(u => u.InventoryUpdateType, f => f.Random.Int(1, 100))
					.RuleFor(u => u.InventoryUpdateStatus, f => f.Random.Int(1, 100))
					.RuleFor(u => u.UpdateDate, f => f.Date.Past(0).Date)
					.RuleFor(u => u.UpdateTime, f => f.Date.Timespan())
					.RuleFor(u => u.Processor, f => f.Random.AlphaNumeric(50))
					.RuleFor(u => u.WarehouseUuid, f => f.Random.Guid().ToString())
					.RuleFor(u => u.WarehouseCode, f => f.Lorem.Word())
					.RuleFor(u => u.CustomerUuid, f => f.Random.Guid().ToString())
					.RuleFor(u => u.CustomerCode, f => f.Lorem.Word())
					.RuleFor(u => u.CustomerName, f => f.Company.CompanyName())
					.RuleFor(u => u.VendorUuid, f => f.Random.Guid().ToString())
					.RuleFor(u => u.VendorCode, f => f.Lorem.Word())
					.RuleFor(u => u.VendorName, f => f.Company.CompanyName())
					.RuleFor(u => u.ReferenceType, f => f.Random.Int(1, 100))
					.RuleFor(u => u.ReferenceUuid, f => f.Random.Guid().ToString())
					.RuleFor(u => u.ReferenceNum, f => f.Random.AlphaNumeric(50))
					.RuleFor(u => u.InventoryUpdateSourceCode, f => f.Lorem.Word())
					.RuleFor(u => u.UpdateDateUtc, f => f.Date.Past(0).Date)
					.RuleFor(u => u.EnterBy, f => f.Lorem.Sentence().TruncateTo(100))
					.RuleFor(u => u.UpdateBy, f => f.Lorem.Sentence().TruncateTo(100))
					;
            #endregion faker data rules
        }

        protected const string SkipReason = "Debug Helper Function";

        protected TestFixture<StartupTest> Fixture { get; }
        public IConfiguration Configuration { get; }
        public Faker<InventoryUpdateHeader> FakerData { get; set; }
        public IDataBaseFactory DataBaseFactory { get; set; }

        public InventoryUpdateHeaderTests(TestFixture<StartupTest> fixture) 
        {
            Fixture = fixture;
            Configuration = fixture.Configuration;

            InitForTest();
        }
        protected void InitForTest()
        {
            var Seq = 0;
            DataBaseFactory = new DataBaseFactory(Configuration["dsn"]);
            FakerData = GetFakerData();
        }
        public void Dispose()
        {
        }

        //[Fact()]
        [Fact(Skip = SkipReason)]
        public void Register_Test()
        {
            var data = FakerData.Generate();
            data.Register();
            var poco = data.GetPocoData();

            Assert.True(true, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void CopyFrom_Test()
        {
            var data = FakerData.Generate();
            var dataJson = data.ObjectToString();
            var newData = FakerData.Generate();
            var newDataJson = newData.ObjectToString();
            data?.CopyFrom(newData);
            var result = !data.Equals(newData);

			Assert.True(true, "This is a generated tester, please report any tester bug to team leader.");
        }

        #region sync methods

        [Fact()]
		//[Fact(Skip = SkipReason)]
		public void Add_Test()
		{
			var data = FakerData.Generate();
            data.SetDataBaseFactory(DataBaseFactory);
            DataBaseFactory.Begin();
			data.Add();
            DataBaseFactory.Commit();

            var dataGet = DataBaseFactory.GetFromCacheById<InventoryUpdateHeader>(data.UniqueId);
            var result = data.Equals(dataGet);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Put_Test()
        {
            var list = DataBaseFactory.Find<InventoryUpdateHeader>("SELECT TOP 1 * FROM InventoryUpdateHeader").ToList();

            DataBaseFactory.Begin();
            var data = list.FirstOrDefault();
            data.SetDataBaseFactory(DataBaseFactory);
            var newData = FakerData.Generate();
            data?.CopyFrom(newData);
            data.Put();
            DataBaseFactory.Commit();

            var dataGet = DataBaseFactory.GetFromCacheById<InventoryUpdateHeader>(data.UniqueId);
            var result = data.Equals(dataGet);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Patch_Test()
        {
            var list = DataBaseFactory.Find<InventoryUpdateHeader>("SELECT TOP 1 * FROM InventoryUpdateHeader").ToList();

            DataBaseFactory.Begin();
            var data = list.FirstOrDefault();
            var dataOrig = new InventoryUpdateHeader();
            dataOrig?.CopyFrom(data);

            data.SetDataBaseFactory(DataBaseFactory);
            var newData = FakerData.Generate();
            data?.CopyFrom(newData);
            data.Patch(new[] { "BatchNumber", "Processor" });
            DataBaseFactory.Commit();

            var dataGet = DataBaseFactory.GetFromCache<InventoryUpdateHeader>(data.RowNum);
            var result = dataGet.BatchNumber != dataOrig.BatchNumber &&
                            dataGet.Processor != dataOrig.Processor &&
                            dataGet.BatchNumber == newData.BatchNumber &&
                            dataGet.Processor == newData.Processor;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Save_Test()
        {
	        var dataNew = FakerData.Generate();
            dataNew.SetDataBaseFactory(DataBaseFactory);
            DataBaseFactory.Begin();
            dataNew.Save();
            DataBaseFactory.Commit();

            var dataUpdate = DataBaseFactory.GetById<InventoryUpdateHeader>(dataNew.UniqueId);
			var dataChanged = FakerData.Generate();
            dataUpdate?.CopyFrom(dataChanged, new[] {"InventoryUpdateUuid"});

            DataBaseFactory.Begin();
            dataUpdate.Save();
            DataBaseFactory.Commit();

            var dataGet = DataBaseFactory.GetFromCacheById<InventoryUpdateHeader>(dataUpdate.UniqueId);
            var result = dataUpdate.Equals(dataGet);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Delete_Test()
        { 
            var list = DataBaseFactory.Find<InventoryUpdateHeader>("SELECT TOP 1 * FROM InventoryUpdateHeader").ToList();
            var data = list.FirstOrDefault();

            DataBaseFactory.Begin();
            data.SetDataBaseFactory(DataBaseFactory);
            data.Delete();
            DataBaseFactory.Commit();

            var result = DataBaseFactory.ExistUniqueId<InventoryUpdateHeader>(data.UniqueId);

            Assert.True(!result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Get_Test()
        {
            //var list = DataBaseFactory.Find<InventoryUpdateHeader>().ToList();
            //var listData = list.FirstOrDefault();
            //var data = DataBaseFactory.Get<InventoryUpdateHeader>(listData.RowNum);
            //var result = data.Equals(listData);

            var list = DataBaseFactory.Find<InventoryUpdateHeader>("SELECT TOP 1 * FROM InventoryUpdateHeader").ToList();
            var listData = list.FirstOrDefault(); 
            var result = listData!=null;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void GetById_Test()
        {
            var list = DataBaseFactory.Find<InventoryUpdateHeader>("SELECT TOP 1 * FROM InventoryUpdateHeader").ToList();
            var listData = list.FirstOrDefault();
            var data = DataBaseFactory.GetById<InventoryUpdateHeader>(listData.UniqueId);
            var result = data.Equals(listData);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void AddList_Test()
        {
            var list = FakerData.Generate(10);
            var WarehouseUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.WarehouseUuid = WarehouseUuid);
            list.SetDataBaseFactory<InventoryUpdateHeader>(DataBaseFactory)
                .Save<InventoryUpdateHeader>();

            var cnt = DataBaseFactory.Count<InventoryUpdateHeader>("WHERE WarehouseUuid = @0", WarehouseUuid);
            var result = cnt.Equals(list.Count());

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void SaveList_Test()
        {
            var list = FakerData.Generate(10);
            var WarehouseUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.WarehouseUuid = WarehouseUuid);
            list.SetDataBaseFactory<InventoryUpdateHeader>(DataBaseFactory)
                .Save<InventoryUpdateHeader>();

            var NewProcessor = Guid.NewGuid().ToString();
            var listFind = DataBaseFactory.Find<InventoryUpdateHeader>("WHERE WarehouseUuid = @0 ORDER BY RowNum", WarehouseUuid).ToList();
            listFind.ToList().ForEach(x => x.Processor = NewProcessor);
            listFind.Save<InventoryUpdateHeader>();

            list = DataBaseFactory.Find<InventoryUpdateHeader>("WHERE WarehouseUuid = @0 ORDER BY RowNum", WarehouseUuid).ToList();
            var result = list.Where(x => x.Processor == NewProcessor).Count() == listFind.Count();

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void DeleteList_Test()
        {
            var list = FakerData.Generate(10);
            var WarehouseUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.WarehouseUuid = WarehouseUuid);
            list.SetDataBaseFactory<InventoryUpdateHeader>(DataBaseFactory)
                .Save();

            var listFind = DataBaseFactory.Find<InventoryUpdateHeader>("WHERE WarehouseUuid = @0 ORDER BY RowNum", WarehouseUuid).ToList();
            listFind.Delete();

            var cnt = DataBaseFactory.Count<InventoryUpdateHeader>("WHERE WarehouseUuid = @0", WarehouseUuid);
            var result = cnt == 0;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void GetFromCacheById_Test()
        {
            var list = DataBaseFactory.Find<InventoryUpdateHeader>("SELECT TOP 1 * FROM InventoryUpdateHeader").ToList();
            var data = list.FirstOrDefault();
            var data1 = DataBaseFactory.GetFromCacheById<InventoryUpdateHeader>(data.UniqueId);
            var data2 = DataBaseFactory.GetFromCacheById<InventoryUpdateHeader>(data.UniqueId);

            var result = data1 == data2;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        #endregion sync methods

        #region async methods

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task AddAsync_Test()
        {
            var data = FakerData.Generate();
            data.SetDataBaseFactory(DataBaseFactory);
            DataBaseFactory.Begin();
            await data.AddAsync();
            DataBaseFactory.Commit();

            var dataGet = await DataBaseFactory.GetFromCacheByIdAsync<InventoryUpdateHeader>(data.UniqueId);
            var result = data.Equals(dataGet);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task PutAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InventoryUpdateHeader>()).ToList();

            DataBaseFactory.Begin();
            var data = list.FirstOrDefault();
            data.SetDataBaseFactory(DataBaseFactory);
            var newData = FakerData.Generate();
            data?.CopyFrom(newData);
            await data.PutAsync();
            DataBaseFactory.Commit();

            var dataGet = await DataBaseFactory.GetFromCacheByIdAsync<InventoryUpdateHeader>(data.UniqueId);
            var result = data.Equals(dataGet);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task PatchAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InventoryUpdateHeader>()).ToList();

            DataBaseFactory.Begin();
            var data = list.FirstOrDefault();
            var dataOrig = new InventoryUpdateHeader();
            dataOrig?.CopyFrom(data);

            data.SetDataBaseFactory(DataBaseFactory);
            var newData = FakerData.Generate();
            data?.CopyFrom(newData);
            await data.PatchAsync(new[] { "BatchNumber", "Processor" });
            DataBaseFactory.Commit();

            var dataGet = await DataBaseFactory.GetFromCacheAsync<InventoryUpdateHeader>(data.RowNum);
            var result = dataGet.BatchNumber != dataOrig.BatchNumber &&
                            dataGet.Processor != dataOrig.Processor &&
                            dataGet.BatchNumber == newData.BatchNumber &&
                            dataGet.Processor == newData.Processor;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task SaveAsync_Test()
        {
            var dataNew = FakerData.Generate();
            dataNew.SetDataBaseFactory(DataBaseFactory);
            DataBaseFactory.Begin();
            await dataNew.SaveAsync();
            DataBaseFactory.Commit();

            var dataUpdate = await DataBaseFactory.GetByIdAsync<InventoryUpdateHeader>(dataNew.UniqueId);
            var dataChanged = FakerData.Generate();
            dataUpdate?.CopyFrom(dataChanged, new[] { "InventoryUpdateUuid" });

            DataBaseFactory.Begin();
            await dataUpdate.SaveAsync();
            DataBaseFactory.Commit();

            var dataGet = await DataBaseFactory.GetFromCacheByIdAsync<InventoryUpdateHeader>(dataUpdate.UniqueId);
            var result = dataUpdate.Equals(dataGet);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task DeleteAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InventoryUpdateHeader>()).ToList();
            var data = list.FirstOrDefault();

            DataBaseFactory.Begin();
            data.SetDataBaseFactory(DataBaseFactory);
            await data.DeleteAsync();
            DataBaseFactory.Commit();

            var result = await DataBaseFactory.ExistUniqueIdAsync<InventoryUpdateHeader>(data.UniqueId);

            Assert.True(!result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task GetAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InventoryUpdateHeader>()).ToList();
            var listData = list.FirstOrDefault();
            var data = await DataBaseFactory.GetAsync<InventoryUpdateHeader>(listData.RowNum);
            var result = data.Equals(listData);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task GetByIdAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InventoryUpdateHeader>()).ToList();
            var listData = list.FirstOrDefault();
            var data = await DataBaseFactory.GetByIdAsync<InventoryUpdateHeader>(listData.UniqueId);
            var result = data.Equals(listData);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task AddListAsync_Test()
        {
            var list = FakerData.Generate(10);
            var WarehouseUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.WarehouseUuid = WarehouseUuid);
            await list
                .SetDataBaseFactory<InventoryUpdateHeader>(DataBaseFactory)
                .SaveAsync<InventoryUpdateHeader>();

            var cnt = await DataBaseFactory.CountAsync<InventoryUpdateHeader>("WHERE WarehouseUuid = @0", WarehouseUuid);
            var result = cnt.Equals(list.Count());

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task SaveListAsync_Test()
        {
            var list = FakerData.Generate(10);
            var WarehouseUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.WarehouseUuid = WarehouseUuid);
            await list
                .SetDataBaseFactory<InventoryUpdateHeader>(DataBaseFactory)
                .SaveAsync<InventoryUpdateHeader>();

            var NewProcessor = Guid.NewGuid().ToString();
            var listFind = (await DataBaseFactory.FindAsync<InventoryUpdateHeader>("WHERE WarehouseUuid = @0 ORDER BY RowNum", WarehouseUuid)).ToList();
            listFind.ToList().ForEach(x => x.Processor = NewProcessor);
            await listFind.SaveAsync<InventoryUpdateHeader>();

            list = DataBaseFactory.Find<InventoryUpdateHeader>("WHERE WarehouseUuid = @0 ORDER BY RowNum", WarehouseUuid).ToList();
            var result = list.Where(x => x.Processor == NewProcessor).Count() == listFind.Count();

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task DeleteListAsync_Test()
        {
            var list = FakerData.Generate(10);
            var WarehouseUuid = Guid.NewGuid().ToString();

            list.ForEach(x => x.WarehouseUuid = WarehouseUuid);
            await list
                .SetDataBaseFactory<InventoryUpdateHeader>(DataBaseFactory)
                .SaveAsync();

            var listFind = (await DataBaseFactory.FindAsync<InventoryUpdateHeader>("WHERE WarehouseUuid = @0 ORDER BY RowNum", WarehouseUuid)).ToList();
            await listFind.DeleteAsync();

            var cnt = await DataBaseFactory.CountAsync<InventoryUpdateHeader>("WHERE WarehouseUuid = @0", WarehouseUuid);
            var result = cnt == 0;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task GetFromCacheByIdAsync_Test()
        {
            var list = (await DataBaseFactory.FindAsync<InventoryUpdateHeader>("SELECT TOP 1 * FROM InventoryUpdateHeader")).ToList();
            var data = list.FirstOrDefault();
            var data1 = await DataBaseFactory.GetFromCacheByIdAsync<InventoryUpdateHeader>(data.UniqueId);
            var data2 = await DataBaseFactory.GetFromCacheByIdAsync<InventoryUpdateHeader>(data.UniqueId);

            var result = data1 == data2;

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        #endregion sync methods

    }
}


