    

//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;
using System.Threading.Tasks;
using Microsoft.Extensions.Configuration;
using Xunit;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.XUnit.Common;
using DigitBridge.CommerceCentral.ERPDb;
using Bogus;

namespace DigitBridge.CommerceCentral.ERPMdl.Tests.Integration
{
    public partial class ApInvoiceServiceTests
    {


        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task GetInitNumber_Test()
        {
            var srv = new ApInvoiceManager(DataBaseFactory);
            string iniNumber = await srv.GetNextNumberAsync(10001, 10001, "eadf5c15-3702-ff74-7d68-5be78956ad45");
            Assert.True(true, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task UpdateInitNumber_Test()
        {
            var srv = new ApInvoiceManager(DataBaseFactory);
              await srv.UpdateInitNumberForCustomerAsync(10001, 10001, "eadf5c15-3702-ff74-7d68-5be78956ad45","10001");
            Assert.True(true, "This is a generated tester, please report any tester bug to team leader.");
        }


        [Fact()]
		//[Fact(Skip = SkipReason)]
		public void AddDto_Test()
		{
            var srv = new ApInvoiceService(DataBaseFactory);
            srv.Add();

            var mapper = srv.DtoMapper;
            var data = GetFakerData();
            var dto = mapper.WriteDto(data, null);
            

            srv.Add(dto);

            var id = dto.ApInvoiceHeader.ApInvoiceUuid;

            var srvGet = new ApInvoiceService(DataBaseFactory);
            //srvGet.Edit();
            srvGet.GetDataById(id);
            var result = srv.Data.Equals(srvGet.Data);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}

        [Fact()]
		//[Fact(Skip = SkipReason)]
		public void UpdateDto_Test()
		{
            SaveData_Test();

            var id = DataBaseFactory.GetValue<ApInvoiceHeader, string>(@"
SELECT TOP 1 ins.ApInvoiceUuid 
FROM ApInvoiceHeader ins 
INNER JOIN (
    SELECT it.ApInvoiceUuid, COUNT(1) AS cnt FROM ApInvoiceItems it GROUP BY it.ApInvoiceUuid
) itm ON (itm.ApInvoiceUuid = ins.ApInvoiceUuid)
WHERE itm.cnt > 0
");


            var srv = new ApInvoiceService(DataBaseFactory);
            srv.Edit(id);
            var rowNum = srv.Data.ApInvoiceHeader.RowNum;

            var mapper = srv.DtoMapper;
            var data = GetFakerData();
            var dto = mapper.WriteDto(data, null);
            dto.ApInvoiceHeader.RowNum = rowNum;
            dto.ApInvoiceHeader.ApInvoiceUuid = id;

            srv.Clear();
            srv.Update(dto);

            var srvGet = new ApInvoiceService(DataBaseFactory);
            srvGet.Edit();
            srvGet.GetDataById(id);
            var result = srv.Data.Equals(srvGet.Data);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}

        [Fact()]
		//[Fact(Skip = SkipReason)]
		public async Task AddDtoAsync_Test()
		{
            var srv = new ApInvoiceService(DataBaseFactory);
            srv.Add();

            var mapper = srv.DtoMapper;
            var data = GetFakerData();
            var dto = mapper.WriteDto(data, null); 

            await srv.AddAsync(dto);

            var id = dto.ApInvoiceHeader.ApInvoiceUuid;

            var srvGet = new ApInvoiceService(DataBaseFactory);
            srvGet.Edit();
            await srvGet.GetDataByIdAsync(id);
            var result = srv.Data.Equals(srvGet.Data);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}

        [Fact()]
		//[Fact(Skip = SkipReason)]
		public async Task UpdateDtoAsync_Test()
		{
            await SaveDataAsync_Test();

            var id = await DataBaseFactory.GetValueAsync<ApInvoiceHeader, string>(@"
SELECT TOP 1 ins.ApInvoiceUuid 
FROM ApInvoiceHeader ins 
INNER JOIN (
    SELECT it.ApInvoiceUuid, COUNT(1) AS cnt FROM ApInvoiceItems it GROUP BY it.ApInvoiceUuid
) itm ON (itm.ApInvoiceUuid = ins.ApInvoiceUuid)
WHERE itm.cnt > 0
");


            var srv = new ApInvoiceService(DataBaseFactory);
            await srv.EditAsync(id);
            var rowNum = srv.Data.ApInvoiceHeader.RowNum;

            var mapper = srv.DtoMapper;
            var data = GetFakerData();
            var dto = mapper.WriteDto(data, null);
            dto.ApInvoiceHeader.RowNum = rowNum;
            dto.ApInvoiceHeader.ApInvoiceUuid = id;

            srv.Clear();
            await srv.UpdateAsync(dto);

            var srvGet = new ApInvoiceService(DataBaseFactory);
            //srvGet.Edit();
            await srvGet.GetDataByIdAsync(id);
            var result = srv.Data.Equals(srvGet.Data);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}


        [Fact()]
		//[Fact(Skip = SkipReason)]
		public void AddPayload_Test()
		{
            var srv = new ApInvoiceService(DataBaseFactory);
            srv.Add();

            var mapper = srv.DtoMapper;
            var data = GetFakerData();
            var dto = mapper.WriteDto(data, null);
            

            var payload = new ApInvoicePayload();
            payload.ApInvoice = dto;
            payload.MasterAccountNum = 1;
            payload.ProfileNum = 1;
            payload.DatabaseNum = 1;

            srv.Add(payload);
            var id = dto.ApInvoiceHeader.ApInvoiceUuid;
            var srvGet = new ApInvoiceService(DataBaseFactory);
            //srvGet.Edit();
            srvGet.GetDataById(id);
            var result = srv.Data.Equals(srvGet.Data);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}

        [Fact()]
		//[Fact(Skip = SkipReason)]
		public void UpdatePayload_Test()
		{
            SaveData_Test();

            var id = DataBaseFactory.GetValue<ApInvoiceHeader, string>(@"
SELECT TOP 1 ins.ApInvoiceUuid 
FROM ApInvoiceHeader ins 
INNER JOIN (
    SELECT it.ApInvoiceUuid, COUNT(1) AS cnt FROM ApInvoiceItems it GROUP BY it.ApInvoiceUuid
) itm ON (itm.ApInvoiceUuid = ins.ApInvoiceUuid)
WHERE itm.cnt > 0
");


            var srv = new ApInvoiceService(DataBaseFactory);
            srv.Edit(id);
            var rowNum = srv.Data.ApInvoiceHeader.RowNum;

            var mapper = srv.DtoMapper;
            var data = GetFakerData();
            var dto = mapper.WriteDto(data, null);
            dto.ApInvoiceHeader.RowNum = rowNum;
            dto.ApInvoiceHeader.ApInvoiceUuid = id;

            var payload = new ApInvoicePayload();
            payload.ApInvoice = dto;
            payload.MasterAccountNum = srv.Data.ApInvoiceHeader.MasterAccountNum;
            payload.ProfileNum = srv.Data.ApInvoiceHeader.ProfileNum;
            payload.DatabaseNum = srv.Data.ApInvoiceHeader.DatabaseNum;

            srv.Clear();
            srv.Update(payload);

            var srvGet = new ApInvoiceService(DataBaseFactory);
            srvGet.Edit();
            srvGet.GetDataById(id);
            var result = srv.Data.Equals(srvGet.Data);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}

        [Fact()]
		//[Fact(Skip = SkipReason)]
		public async Task AddPayloadAsync_Test()
		{
            var srv = new ApInvoiceService(DataBaseFactory);
            srv.Add();

            var mapper = srv.DtoMapper;
            var data = GetFakerData();
            var dto = mapper.WriteDto(data, null);
            
            var payload = new ApInvoicePayload();
            payload.ApInvoice = dto;
            payload.MasterAccountNum = 1;
            payload.ProfileNum = 1;
            payload.DatabaseNum = 1;

            await srv.AddAsync(payload);

            var id = dto.ApInvoiceHeader.ApInvoiceUuid;

            var srvGet = new ApInvoiceService(DataBaseFactory);
            srvGet.Edit();
            await srvGet.GetDataByIdAsync(id);
            var result = srv.Data.Equals(srvGet.Data);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}

        [Fact()]
		//[Fact(Skip = SkipReason)]
		public async Task UpdatePayloadAsync_Test()
		{
            await SaveDataAsync_Test();

            var id = await DataBaseFactory.GetValueAsync<ApInvoiceHeader, string>(@"
SELECT TOP 1 ins.ApInvoiceUuid 
FROM ApInvoiceHeader ins 
INNER JOIN (
    SELECT it.ApInvoiceUuid, COUNT(1) AS cnt FROM ApInvoiceItems it GROUP BY it.ApInvoiceUuid
) itm ON (itm.ApInvoiceUuid = ins.ApInvoiceUuid)
WHERE itm.cnt > 0
");


            var srv = new ApInvoiceService(DataBaseFactory);
            await srv.EditAsync(id);
            var rowNum = srv.Data.ApInvoiceHeader.RowNum;

            var mapper = srv.DtoMapper;
            var data = GetFakerData();
            var dto = mapper.WriteDto(data, null);
            dto.ApInvoiceHeader.RowNum = rowNum;
            dto.ApInvoiceHeader.ApInvoiceUuid = id;

            var payload = new ApInvoicePayload();
            payload.ApInvoice = dto;
            payload.MasterAccountNum = srv.Data.ApInvoiceHeader.MasterAccountNum;
            payload.ProfileNum = srv.Data.ApInvoiceHeader.ProfileNum;
            payload.DatabaseNum = srv.Data.ApInvoiceHeader.DatabaseNum;

            srv.Clear();
            await srv.UpdateAsync(payload);

            var srvGet = new ApInvoiceService(DataBaseFactory);
            //srvGet.Edit();
            await srvGet.GetDataByIdAsync(id);
            var result = srv.Data.Equals(srvGet.Data);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}

    }
}



