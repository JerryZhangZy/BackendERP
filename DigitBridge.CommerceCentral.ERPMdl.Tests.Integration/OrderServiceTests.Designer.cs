
              
    

//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;
using System.Threading.Tasks;
using Microsoft.Extensions.Configuration;
using Xunit;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.XUnit.Common;
using DigitBridge.CommerceCentral.ERPDb.Tests.Integration;
using DigitBridge.CommerceCentral.ERPDb;

namespace DigitBridge.CommerceCentral.ERPMdl.Tests.Integration
{
    /// <summary>
    /// Represents a Tester for OrderService.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public partial class OrderServiceTests : IDisposable, IClassFixture<TestFixture<StartupTest>>
    {
        protected OrderData GetFakerData()
        {
            return OrderDataTests.GetFakerData();
        }

        protected List<OrderData> GetFakerData(int count)
        {
            return OrderDataTests.GetFakerData(count);
        }

        protected const string SkipReason = "Debug Helper Function";

        protected TestFixture<StartupTest> Fixture { get; }
        public IConfiguration Configuration { get; }
        public IDataBaseFactory DataBaseFactory { get; set; }

        public OrderServiceTests(TestFixture<StartupTest> fixture) 
        {
            Fixture = fixture;
            Configuration = fixture.Configuration;

            InitForTest();
        }
        protected void InitForTest()
        {
            var Seq = 0;
            DataBaseFactory = new DataBaseFactory(Configuration["dsn"]);
        }
        public void Dispose()
        {
        }

        #region sync methods

        [Fact()]
		//[Fact(Skip = SkipReason)]
		public void Save_Test()
		{
            var srv = new OrderService(DataBaseFactory);
            srv.AttachData(GetFakerData());
			srv.Save();

            var srvGet = new OrderService(DataBaseFactory);
            srvGet.GetById(srv.Data.UniqueId);
            var result = srv.Data.Equals(srvGet.Data);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Get_Test()
        {
            var id = DataBaseFactory.GetValue<OrderHeader, string>(@"
SELECT TOP 1 ins.OrderUuid 
FROM OrderHeader ins 
INNER JOIN (
    SELECT it.OrderUuid, COUNT(1) AS cnt FROM OrderItems it GROUP BY it.OrderUuid
) itm ON (itm.OrderUuid = ins.OrderUuid)
WHERE itm.cnt > 0
");


            var srv = new OrderService(DataBaseFactory);
            srv.GetById(id);
            var rowNum = srv.Data.OrderHeader.RowNum;

            var dataUpdate = GetFakerData();
            srv.Data.CopyFrom(dataUpdate);
            srv.Save();

            var srvGetById = new OrderService(DataBaseFactory);
            srvGetById.GetById(id);

            var srvGet = new OrderService(DataBaseFactory);
            srvGet.Get(rowNum);

            var result = srv.Data.Equals(srvGet.Data) && srvGet.Data.Equals(srvGetById.Data);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public void Delete_Test()
        {
            var id = DataBaseFactory.GetValue<OrderHeader, string>(@"
SELECT TOP 1 ins.OrderUuid 
FROM OrderHeader ins 
INNER JOIN (
    SELECT it.OrderUuid, COUNT(1) AS cnt FROM OrderItems it GROUP BY it.OrderUuid
) itm ON (itm.OrderUuid = ins.OrderUuid)
WHERE itm.cnt > 0
");


            var srv = new OrderService(DataBaseFactory);
            srv.GetById(id);
            srv.Delete();

            var result = DataBaseFactory.ExistUniqueId<OrderHeader>(srv.Data.UniqueId);

            Assert.True(!result, "This is a generated tester, please report any tester bug to team leader.");
        }

        #endregion sync methods

        #region async methods

        [Fact()]
		//[Fact(Skip = SkipReason)]
		public async Task SaveAsync_Test()
		{
            var srv = new OrderService(DataBaseFactory);
            srv.AttachData(GetFakerData());
			await srv.SaveAsync();

            var srvGet = new OrderService(DataBaseFactory);
            await srvGet.GetByIdAsync(srv.Data.UniqueId);
            var result = srv.Data.Equals(srvGet.Data);

			Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
		}

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task GetAsync_Test()
        {
            var id = await DataBaseFactory.GetValueAsync<OrderHeader, string>(@"
SELECT TOP 1 ins.OrderUuid 
FROM OrderHeader ins 
INNER JOIN (
    SELECT it.OrderUuid, COUNT(1) AS cnt FROM OrderItems it GROUP BY it.OrderUuid
) itm ON (itm.OrderUuid = ins.OrderUuid)
WHERE itm.cnt > 0
");


            var srv = new OrderService(DataBaseFactory);
            await srv.GetByIdAsync(id);
            var rowNum = srv.Data.OrderHeader.RowNum;

            var dataUpdate = GetFakerData();
            srv.Data.CopyFrom(dataUpdate);
            await srv.SaveAsync();

            var srvGetById = new OrderService(DataBaseFactory);
            await srvGetById.GetByIdAsync(id);

            var srvGet = new OrderService(DataBaseFactory);
            await srvGet.GetAsync(rowNum);

            var result = srv.Data.Equals(srvGet.Data) && srvGet.Data.Equals(srvGetById.Data);

            Assert.True(result, "This is a generated tester, please report any tester bug to team leader.");
        }

        [Fact()]
        //[Fact(Skip = SkipReason)]
        public async Task DeleteAsync_Test()
        {

            var id = await DataBaseFactory.GetValueAsync<OrderHeader, string>(@"
SELECT TOP 1 ins.OrderUuid 
FROM OrderHeader ins 
INNER JOIN (
    SELECT it.OrderUuid, COUNT(1) AS cnt FROM OrderItems it GROUP BY it.OrderUuid
) itm ON (itm.OrderUuid = ins.OrderUuid)
WHERE itm.cnt > 0
");


            var srv = new OrderService(DataBaseFactory);
            await srv.GetByIdAsync(id);
            await srv.DeleteAsync();

            var result = DataBaseFactory.ExistUniqueId<OrderHeader>(srv.Data.UniqueId);

            Assert.True(!result, "This is a generated tester, please report any tester bug to team leader.");
        }

        #endregion async methods

    }
}


