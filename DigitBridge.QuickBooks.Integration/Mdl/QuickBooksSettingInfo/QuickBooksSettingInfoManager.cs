//-------------------------------------------------------------------------
// This document is generated by T4
// It will overwrite your changes, please keep it as it is
// <copyright company="DigitBridge">
//     Copyright (c) DigitBridge Inc.  All rights reserved.
// </copyright>
//-------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Web;
using System.Threading.Tasks;
using System.Xml.Serialization;
using Newtonsoft.Json;

using DigitBridge.Base.Utility;
using DigitBridge.CommerceCentral.YoPoco;
using DigitBridge.CommerceCentral.ERPDb;
using Microsoft.AspNetCore.Http;

namespace DigitBridge.QuickBooks.Integration
{
    /// <summary>
    /// Represents a QuickBooksSettingInfoService.
    /// NOTE: This class is generated from a T4 template - you should not modify it manually.
    /// </summary>
    public class QuickBooksSettingInfoManager :  IQuickBooksSettingInfoManager , IMessage
    {

        public QuickBooksSettingInfoManager() : base() {}

        public QuickBooksSettingInfoManager(IDataBaseFactory dbFactory)
        {
            SetDataBaseFactory(dbFactory);
        }
        
        [XmlIgnore, JsonIgnore]
        protected QuickBooksSettingInfoService _quickBooksSettingInfoService;
        [XmlIgnore, JsonIgnore]
        public QuickBooksSettingInfoService quickBooksSettingInfoService
        {
            get
            {
                if (_quickBooksSettingInfoService is null)
                    _quickBooksSettingInfoService = new QuickBooksSettingInfoService(dbFactory);
                return _quickBooksSettingInfoService;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected QuickBooksSettingInfoDataDtoCsv _quickBooksSettingInfoDataDtoCsv;
        [XmlIgnore, JsonIgnore]
        public QuickBooksSettingInfoDataDtoCsv quickBooksSettingInfoDataDtoCsv
        {
            get
            {
                if (_quickBooksSettingInfoDataDtoCsv is null)
                    _quickBooksSettingInfoDataDtoCsv = new QuickBooksSettingInfoDataDtoCsv();
                return _quickBooksSettingInfoDataDtoCsv;
            }
        }

        [XmlIgnore, JsonIgnore]
        protected QuickBooksSettingInfoList _quickBooksSettingInfoList;
        [XmlIgnore, JsonIgnore]
        public QuickBooksSettingInfoList quickBooksSettingInfoList
        {
            get
            {
                if (_quickBooksSettingInfoList is null)
                    _quickBooksSettingInfoList = new QuickBooksSettingInfoList(dbFactory);
                return _quickBooksSettingInfoList;
            }
        }

        public async Task<byte[]> ExportAsync(QuickBooksSettingInfoPayload payload)
        {
            var rowNumList =await quickBooksSettingInfoList.GetRowNumListAsync(payload);
            var dtoList = new List<QuickBooksSettingInfoDataDto>();
           foreach(var x in rowNumList)
            {
                if (quickBooksSettingInfoService.GetData(x))
                    dtoList.Add(quickBooksSettingInfoService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new QuickBooksSettingInfoDataDto());
            return quickBooksSettingInfoDataDtoCsv.Export(dtoList);
        }

        public byte[] Export(QuickBooksSettingInfoPayload payload)
        {
            var rowNumList =quickBooksSettingInfoList.GetRowNumList(payload);
            var dtoList = new List<QuickBooksSettingInfoDataDto>();
            foreach (var x in rowNumList)
            {
                if (quickBooksSettingInfoService.GetData(x))
                    dtoList.Add(quickBooksSettingInfoService.ToDto());
            };
            if (dtoList.Count == 0)
                dtoList.Add(new QuickBooksSettingInfoDataDto());
            return quickBooksSettingInfoDataDtoCsv.Export(dtoList);
        }

        public void Import(QuickBooksSettingInfoPayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list = quickBooksSettingInfoDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.QuickBooksSettingInfo = item;
                    if (quickBooksSettingInfoService.Add(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in quickBooksSettingInfoService.Messages)
                            Messages.Add(msg);
                        quickBooksSettingInfoService.Messages.Clear();
                    }
                }
                if (payload.HasQuickBooksSettingInfo)
                    payload.QuickBooksSettingInfo = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        public async Task ImportAsync(QuickBooksSettingInfoPayload payload, IFormFileCollection files)
        {
            if(files==null||files.Count==0)
            {
                AddError("no files upload");
                return;
            }
            foreach(var file in files)
            {
                if (!file.FileName.ToLower().EndsWith("csv"))
                {
                    AddError($"invalid file type:{file.FileName}");
                    continue;
                }
                var list =quickBooksSettingInfoDataDtoCsv.Import(file.OpenReadStream());
                var readcount = list.Count();
                var addsucccount = 0;
                var errorcount = 0;
                foreach(var item in list)
                {
                    payload.QuickBooksSettingInfo = item;
                    if (await quickBooksSettingInfoService.AddAsync(payload))
                        addsucccount++;
                    else
                    {
                        errorcount++;
                        foreach (var msg in quickBooksSettingInfoService.Messages)
                            Messages.Add(msg);
                        quickBooksSettingInfoService.Messages.Clear();
                    }
                }
                if (payload.HasQuickBooksSettingInfo)
                    payload.QuickBooksSettingInfo = null;
                AddInfo($"File:{file.FileName},Read {readcount},Import Succ {addsucccount},Import Fail {errorcount}.");
            }
        }

        #region DataBase
        [XmlIgnore, JsonIgnore]
        protected IDataBaseFactory _dbFactory;

        [XmlIgnore, JsonIgnore]
        public IDataBaseFactory dbFactory
        {
            get
            {
                if (_dbFactory is null)
                    _dbFactory = DataBaseFactory.CreateDefault();
                return _dbFactory;
            }
        }

        public void SetDataBaseFactory(IDataBaseFactory dbFactory)
        {
            _dbFactory = dbFactory;
        }

        #endregion DataBase

        #region Messages
        protected IList<MessageClass> _messages;
        [XmlIgnore, JsonIgnore]
        public virtual IList<MessageClass> Messages
        {
            get
            {
                if (_messages is null)
                    _messages = new List<MessageClass>();
                return _messages;
            }
            set { _messages = value; }
        }
        public IList<MessageClass> AddInfo(string message, string code = null) =>
             Messages.Add(message, MessageLevel.Info, code);
        public IList<MessageClass> AddWarning(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Warning, code);
        public IList<MessageClass> AddError(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Error, code);
        public IList<MessageClass> AddFatal(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Fatal, code);
        public IList<MessageClass> AddDebug(string message, string code = null) =>
            Messages.Add(message, MessageLevel.Debug, code);

        #endregion Messages
    }
}
